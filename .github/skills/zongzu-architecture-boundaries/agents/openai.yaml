interface:
  display_name: "Zongzu Architecture Boundaries"
  short_description: "Keep Zongzu modules, scheduler, saves, performance hot paths, and extensions aligned to ownership seams"
  default_prompt: "Use $zongzu-architecture-boundaries to review this Zongzu implementation task for module ownership, command/query/event seams, scheduler fit, save impact, startup/load cost, performance hot-path budget, and extension safety."

policy:
  allow_implicit_invocation: true
