interface:
  display_name: "Zongzu UI Shell"
  short_description: "Shape Zongzu UI into playable spatial shell surfaces, Unity-safe projections, and bounded shell performance"
  default_prompt: "Use $zongzu-ui-shell to turn this Zongzu UI topic into a whole-shell pass across surface, player job, object grammar, density, ViewModel or adapter ownership, shell performance, and anti-poster corrections."

policy:
  allow_implicit_invocation: true
