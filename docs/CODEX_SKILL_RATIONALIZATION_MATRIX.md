# CODEX_SKILL_RATIONALIZATION_MATRIX

## Purpose

This matrix keeps the Zongzu Codex skill pack aligned with the current repository, external engineering/accessibility norms, and the game's actual direction.

Use it when:
- a prompt names a Zongzu skill but the task spans adjacent systems;
- a task is short or compressed and needs whole-skill expansion;
- a code or design pass risks drifting into a giant world manager, UI rule layer, event-pool design, or detached tactics game;
- an external "best practice" sounds useful but must be translated into Zongzu's modular-monolith and spatialized-shell rules.

External standards calibrate the skill pack; they do not override product or architecture authority. Zongzu remains a deterministic .NET modular monolith with a Unity presentation shell, bounded player leverage, and a Northern Song-inspired living-world direction.

## Current Repo Anchors

- Authoritative simulation lives under `src/` as a .NET modular monolith.
- `Zongzu.Kernel` and simulation modules must not reference Unity APIs.
- Unity host root exists at `unity/Zongzu.UnityShell` with `Assets/`, `Packages/`, and `ProjectSettings/`.
- `src/Zongzu.Presentation.Unity` and `src/Zongzu.Presentation.Unity.ViewModels` are projection/adaptation layers.
- The repo-tracked Zongzu skill pack under `.github/skills` contains all nine Zongzu skills: game design, architecture boundaries, pressure chain, UI shell, ancient China, content authoring, Unity shell, simulation validation, and save/schema.
- Cross-module cooperation uses Query / Command / DomainEvent.
- `MonthlyScheduler` performs deterministic prepare / xun / month / bounded fresh-event drain / projection ordering.
- Public-life/order closure is implemented through v60 on the current mainline: v3-v30 close Order / home-household / owner-lane projection readback; v31 is merge/branch-cleanup evidence; v32-v34 harden event-contract diagnostics; v35 links canal-window pressure to Trade/Order owner lanes; v36 links household burden to Family sponsor pressure; v37-v45 add Office/yamen implementation and readback; v46-v52 close Office owner-lane entry / receipt / residue / no-loop guidance; v53-v60 close Family owner-lane entry / elder explanation / guarantee / lineage-house face / receipt / residue / no-loop readback.
- V46-V52 closes the Office owner-lane readback loop with runtime-only `OfficeLaneEntryReadbackSummary`, `OfficeLaneReceiptClosureSummary`, `OfficeLaneResidueFollowUpSummary`, and `OfficeLaneNoLoopGuardSummary`. It reads structured Office/SocialMemory snapshots only and adds no schema, migration, ledger, UI/Unity authority, or `PersonRegistry` expansion.
- V53-V60 closes the Family owner-lane readback loop with runtime-only `FamilyLaneEntryReadbackSummary`, `FamilyElderExplanationReadbackSummary`, `FamilyGuaranteeReadbackSummary`, `FamilyHouseFaceReadbackSummary`, `FamilyLaneReceiptClosureSummary`, `FamilyLaneResidueFollowUpSummary`, and `FamilyLaneNoLoopGuardSummary`. It reads structured `ClanSnapshot`, `HouseholdPressureSnapshot`, `SponsorClanId`, and SocialMemory cause-key snapshots only; `Family承接入口`, `族老解释读回`, `本户担保读回`, `宗房脸面读回`, `Family后手收口读回`, `Family余味续接读回`, `Family闭环防回压`, and `不是普通家户再扛` are projection/readback guidance, not persisted state, a new command system, an event pool, or thick Family rules.
- V61-V68 turns that Family readback into one thin `FamilyCore`-owned relief choice, `GrantClanRelief`. It spends and eases existing `CharityObligation`, `SupportReserve`, `BranchTension`, `BranchFavorPressure`, `ReliefSanctionPressure`, and `MediationMomentum` fields only; Application catalogs/projects, Unity copies projected command DTOs, `PopulationAndHouseholds` is not mutated, and `SocialMemoryAndRelations` waits for later structured residue. It adds no schema, migration, relief/charity/owner-lane ledger, household target field, thick clan economy, elder AI, branch-faction model, guarantee formula, or `PersonRegistry` expansion.
- V77-V84 deepens the existing WarfareCampaign command lane by making `DraftCampaignPlan`, `CommitMobilization`, `ProtectSupplyLine`, and `WithdrawToBarracks` read as `军令选择读回` / `案头筹议选择` / `点兵加压选择` / `粮道护持选择` / `归营止损选择`. It reuses existing `ActiveDirective*` and `LastDirectiveTrace` fields; Application projects the choice alongside v69-v76 closure guidance, Unity copies it, and no schema, migration, directive ledger, force/campaign closure ledger, Application rule layer, UI authority, or `PersonRegistry` expansion is added.
- V85-V92 exposes existing `WarfareCampaign.AftermathDockets` through runtime read models and makes campaign after-account wording explicit as `战后案卷读回`, `记功簿读回`, `劾责状读回`, `抚恤簿读回`, `清路札读回`, `WarfareCampaign拥有战后案卷`, `战后案卷不是县门/Order代算`, `不是普通家户补战后`, and `军务案卷防回压`. It reads structured `AftermathDocketSnapshot` lists only; Application assembles projection/readback, Unity displays those projected snapshots/fields, and no schema, migration, aftermath ledger, route-repair ledger, relief ledger, Application rule layer, UI authority, or `PersonRegistry` expansion is added.
- V93-V100 makes the existing court-policy process readable as `朝议压力读回`, `政策窗口读回`, `文移到达读回`, `县门执行承接读回`, `公议读法读回`, `Court后手不直写地方`, `Office/PublicLife分读`, `不是本户也不是县门独吞朝廷后账`, and `Court-policy防回压`. It reuses `CourtAgendaPressureAccumulated -> PolicyWindowOpened -> PolicyImplemented -> PublicLifeAndRumor` structured facts and current `JurisdictionAuthoritySnapshot` / `SettlementPublicLifeSnapshot` fields only; Application assembles projection/readback, Unity copies projected fields, and no schema, migration, court module, dispatch ledger, policy closure ledger, Application rule layer, UI authority, or `PersonRegistry` expansion is added.
- V101-V108 is the thin-chain closeout audit. It treats the implemented v3-v100 work as a completed thin skeleton only: topology, owner-lane handoffs, projection/readback, no-loop guidance, UI/Unity copy-only display, no-summary-parsing guards, and no-save/no-schema documentation. It is not a full-chain completion claim and adds no production rule, persisted state, schema, migration, ledger, manager/controller layer, UI authority, or `PersonRegistry` expansion.
- V109-V196 is a narrow Chain 8 first rule-density branch after thin-chain closeout: policy process texture, Office-owned local response, delayed SocialMemory residue, memory-pressure next-window readback, public-reading echo, public follow-up cue, docket guard, suggested-action guard, suggested receipt guard, receipt-docket consistency guard, and public-life receipt echo. V189-V196 adds `公议回执回声防误读` / `街面只读已投影的政策公议后手` / `公议不把回执读成新政令` to projected public-life command readbacks only, with no schema, migration, Court module, receipt/suggested-receipt/receipt-docket/public-life-receipt-echo ledger, Application rule layer, UI/Unity authority, prose parsing, or `PersonRegistry` expansion.
- V197-V204 is the v109-v196 first rule-density closeout audit: it records that the first-layer branch is closed without claiming the full court engine, court process state, appointment slate, dispatch arrival, or downstream household/market/public consequence rules.
- V213-V292 implements and closes the first social mobility / fidelity-ring / influence-readback substrate. It uses existing `PopulationAndHouseholds` and `PersonRegistry.FidelityRing` state/read models, not a full society engine, movement ledger, or direct personnel command system.
- V253-V268 implements and closes the first Chain 9 regime-legitimacy readback layer. It reads existing world/office/public-life pressure and public legitimacy projections, not a full regime-recognition engine or dynasty-cycle simulation.
- V293-V300 adds personnel-command preflight guardrails. It documents and tests that direct move/transfer/summon-style pressure is not yet a command lane, and it adds no persisted state, schema, migration, owner ledger, or `PersonRegistry` expansion.
- V301-V308 adds personnel-flow command readiness readback on existing `PopulationAndHouseholds` public-life local response affordances/receipts. It is runtime projection/ViewModel text only, not a new command, movement system, ledger, schema, or `PersonRegistry` expansion.
- V309-V316 adds a command-surface personnel-flow readiness echo on `PlayerCommandSurfaceSnapshot` and Great Hall mobility readback. It reads only structured command readiness fields and adds no direct personnel command, movement resolver, ledger, schema, prose parser, or `PersonRegistry` expansion.
- V317-V324 closes v293-v316 as a first personnel-flow command-readiness layer only. It is docs/tests only and does not claim a full migration/social-mobility/personnel-command engine.
- V325-V332 adds a personnel-flow owner-lane gate readback on `PlayerCommandSurfaceSnapshot` and Great Hall mobility readback. It names current `PopulationAndHouseholds` ownership and future Family/Office/Warfare owner-lane plans without adding commands, movement authority, schema, ledger, parser, or `PersonRegistry` expansion.
- V333-V340 echoes the personnel-flow owner-lane gate on Desk Sandbox settlement mobility readback only when local projected public-life personnel-flow readiness commands/receipts exist. It is presentation-only and adds no command, movement authority, schema, ledger, parser, or `PersonRegistry` expansion.
- V341-V380 contains personnel-flow owner-lane containment, future Family/Office/Warfare lane preflight, and closeout evidence. It keeps future movement, migration, office-service, kin-transfer, assignment, and campaign-manpower lanes blocked until a real owner command/state plan exists.
- V381-V404 contains commoner/social-position preflight plus runtime `SocialPositionReadbackSummary` and `SocialPositionSourceModuleKeys` on person dossier read models and Unity-facing ViewModels. It reads structured owner-module snapshots only and adds no class engine, promote/demote command, zhuhu/kehu conversion state, ledger/schema, prose parser, Application/UI/Unity authority, or `PersonRegistry` expansion.
- V405-V436 contains closeout, scale-budget readback, source/regional guard evidence, and runtime `SocialPositionScaleBudgetReadbackSummary`. It keeps "near detail, far summary" as projection/readback discipline, not fidelity-ring mutation, global person scanning, target selection, status drift, or regional person browsing.
- V437-V452 contains commoner status owner-lane and fidelity scale-budget preflight. Future status/detail work should start in `PopulationAndHouseholds` unless an ExecPlan proves another owner, and must name target scope, hot path, touched counts, deterministic cap/order, cadence, schema impact, projection fields, and validation lane before implementation.
- V453-V460 contains household mobility dynamics explanation. It projects `MobilityDynamicsExplanationSummary`, `MobilityDynamicsDimensionKeys`, and `HouseholdMobilityDynamicsSummary` from existing household social-pressure signals, not a new mobility algorithm, schema field, ledger, selector, or `PersonRegistry` expansion.
- V461-V468 closes v453-v460 as first-layer household mobility explanation only. It is docs/tests governance, not a migration economy, route-history model, class/status engine, direct movement command, selector, durable residue path, or `PersonRegistry` expansion.
- V469-V476 contains household mobility owner-lane preflight. It keeps `PopulationAndHouseholds` as the default first future owner lane and requires owner state, cadence, target scope, hot path, touched counts, deterministic cap/order, no-touch boundary, schema impact, projection fields, and validation before any real household movement, route-history, status, selector, or durable-residue rule lands.
- V485-V492 closes the household mobility owner-lane preflight as governance only. It must not be treated as household movement implementation, migration economy, route history, selector state, durable residue, status/class drift, fidelity promotion, or UI/Unity authority.
- V501-V508 records household mobility first runtime rule readiness and a hardcoded extraction map. It still adds no runtime movement authority, rules-data loader, schema, ledger, selector, route history, `PersonRegistry` expansion, or Application/UI/Unity rule path.
- V509-V516 records household mobility rules-data contract and validator preflight. It remains docs/tests-only because the repo has no reusable owner-consumed runtime rules-data/content/config pattern, and it adds no loader, validator implementation, rules-data file, schema, runtime plugin system, or Application/UI/Unity rule path.
- V517-V524 records household mobility default rules-data skeleton shape. It remains docs/tests-only for the same no-pattern reason and adds no `content/rules-data`, default file, loader, validator implementation, schema, runtime plugin system, or Application/UI/Unity rule path.
- PopulationAndHouseholds First Hardcoded Rule Extraction Through V532 extracts only the focused member promotion fanout cap into owner-consumed runtime code shape. The default remains 2, invalid data falls back deterministically, `PopulationAndHouseholds` remains the only consumer, and there is still no movement authority, rules-data loader, `content/rules-data`, schema change, runtime plugin system, `PersonRegistry` expansion, or Application/UI/Unity rule path.
- PopulationAndHouseholds First Mobility Runtime Rule Through V540 lands the first tiny owner-lane monthly rule. Skill sequencing keeps it as a bounded pressure nudge over existing `MigrationPools` and household fields, not a movement command, route history, migration economy, class/status engine, persisted state, runtime plugin system, `PersonRegistry` expansion, or Application/UI/Unity rule path.
- Household Mobility First Runtime Rule Closeout Through V548 is docs/tests closeout for the V501-V540 track. Skill sequencing keeps the first runtime rule from becoming a second rule family, movement command, route-history model, migration economy, class/status engine, persisted state, runtime plugin system, `PersonRegistry` expansion, or Application/UI/Unity rule path.
- Household Mobility Runtime Rule Health Evidence Through V556 records diagnostics/readiness evidence for the first monthly rule. Skill sequencing keeps it from becoming fanout widening, long-run saturation tuning, a performance optimization claim, movement command, route-history model, migration economy, class/status engine, persisted diagnostic state, runtime plugin system, `PersonRegistry` expansion, or Application/UI/Unity rule path.
- Household Mobility Runtime Widening Gate Through V564 records the next fanout/formula expansion gate. Skill sequencing keeps it from becoming runtime widening, recovery/decay tuning, counters, caches, a performance optimization claim, movement command, route-history model, migration economy, class/status engine, persisted diagnostic state, runtime plugin system, `PersonRegistry` expansion, or Application/UI/Unity rule path.
- Household Mobility Runtime Touch-Count Proof Through V572 records focused owner-test evidence for the current default touch budget. Skill sequencing keeps it from becoming runtime widening, persisted touch-count state, diagnostics state, performance cache, movement command, route-history model, migration economy, class/status engine, runtime plugin system, `PersonRegistry` expansion, or Application/UI/Unity rule path.
- Household Mobility Rules-Data Fallback Matrix Through V580 records focused fallback evidence for malformed runtime rules-data. Skill sequencing keeps it from becoming a rules-data loader, rules-data file, runtime plugin marketplace, arbitrary script rule, reflection-heavy loading, runtime widening, movement command, route-history model, migration economy, class/status engine, persisted validation state, `PersonRegistry` expansion, or Application/UI/Unity rule path.
- Notifications are read-side projection artifacts, not authority drivers.
- Read-only helpers may normalize traversal of existing read models, but may not become ranking, visibility, scheduler, command, or authority policy.
- The v3-v30 public-life/order arc remains the home-household/order owner-lane base: v3-v5 establish leverage/cost/readback, durable SocialMemory residue, and structured Order refusal traces; v6-v9 add bounded response affordances, owner-owned response traces, SocialMemory soft/hard residue, actor countermoves, and repeat-friction proof; v10-v11 add runtime ordinary-household after-account readback/stakes; v12 adds `PopulationAndHouseholds` schema `3` home-household local response commands/traces; v13-v14 route that aftermath through existing SocialMemory memory and later household repeat-friction paths; v15-v18 add common-household texture, capacity affordance, tradeoff forecast, and short-term receipt readback; v19-v30 add projection-only follow-up, owner-lane return/status/outcome/residue, `现有入口读法`, `后手收口读回`, and `闭环防回压` with no scheduler, schema, or ledger expansion.
- v31 is a mainline merge and branch-cleanup pass for that same v20-v30 arc. It is not a new rule, projection, command, schema, migration, or event-topology pass.
- v32 is a backend event-contract health diagnostic pass. It classifies emitted-but-unconsumed and declared-but-not-emitted `DomainEvent` names as projection-only receipts, future contracts, dormant seeded paths, acceptance-test gaps, alignment bugs, or unclassified debt; it adds no runtime rule, projection surface, command, schema, migration, or event-pool authority.
- v33 is a backend event-contract health gate. The ten-year diagnostic run now fails if current event-contract debt remains `Unclassified`; this is test evidence only and still adds no runtime rule, projection surface, command, schema, migration, or event-pool authority.
- v34 is a backend event-contract health evidence-backlink pass. The diagnostic report now displays structured `owner=<module>` lanes and `evidence=<doc/test backlink>` for classified debt; it is still runtime/test evidence only and adds no runtime rule, projection surface, command, schema, migration, event ledger, or event-pool authority.
- v35 graduates `WorldSettlements.CanalWindowChanged` from future-contract debt into a thin owner-lane handoff: `TradeAndIndustry` adjusts existing market/route/black-route ledger state, and `OrderAndBanditry` adjusts existing route/order pressure state. It uses structured canal-window metadata and `IWorldSettlementsQueries`; it adds no schema, migration, event pool, canal ledger, owner-lane ledger, or UI authority.
- v36 graduates household burden facts into a thin `PopulationAndHouseholds` -> `FamilyCore` handoff: `FamilyCore` consumes existing household debt/subsistence/burden events, reads the household through `IPopulationAndHouseholdsQueries`, targets `SponsorClanId`, and adjusts existing family pressure fields. It adds no schema, migration, event pool, relief ledger, sponsor-lane ledger, household target field, SocialMemory write path, or UI authority.
- v37 deepens Renzong chain 8 inside `OfficeAndCareer`: `PolicyWindowOpened` now drains into `PolicyImplemented` through existing office/yamen fields and structured `PolicyWindow*` metadata. It adds no schema, migration, policy ledger, yamen workflow state, Application/UI/Unity authority, or summary parsing.
- Player-command resolution for migrated command lanes uses module-owned `HandleCommand(...)` overrides. `PlayerCommandService` and `PlayerCommandCatalog` route and describe commands; they do not own consequence formulas.
- `PresentationReadModelBundle.SocialMemories`, player-command `LeverageSummary` / `CostSummary` / `ReadbackSummary`, and governance recent-receipt readback are projection/read-model facts. Unity and shell adapters may copy them, not infer or repair them.
- `PresentationReadModelBuilder.PlayerCommands.OwnerLaneReturnSurface` is a projection helper over existing structured snapshots. It may show owner-lane return guidance, `承接入口`, `归口状态`, `归口后读法`, `社会余味读回`, residue follow-up cues, `现有入口读法`, `后手收口读回`, and `闭环防回压`; it must not parse summaries, own command outcomes, create a cooldown ledger, create a follow-up ledger, or create an owner-lane ledger.

## External Calibration Sources

| Source | What It Calibrates | Zongzu Translation |
| --- | --- | --- |
| [Unity Manual: Assembly definitions](https://docs.unity.cn/2023.2/Documentation/Manual/assembly-definition-files.html) | Assembly boundaries and iteration-time dependency control. | Unity shell code can use assembly boundaries for editor/runtime organization, but authority still stays in pure C# simulation modules. |
| [Unity Manual: Asset Metadata](https://docs.unity.cn/Manual/AssetMetadata.html) | `.meta` files, asset IDs, import settings, and version-control hazards. | Generated Unity art and shell assets must keep provenance, manifests, and matching `.meta` discipline; generated assets never become simulation authority. |
| [Unity: Organizing your project](https://unity.com/how-to/organizing-your-project) | Unity project folder consistency, `.meta` file discipline, naming conventions. | Keep `unity/Zongzu.UnityShell` organized for scenes/assets/prefabs while preserving repo/module ownership outside Unity. |
| [Unity: Project configuration and assets](https://unity.com/how-to/project-configuration-and-assets) | Asset and project performance configuration. | Apply to presentation assets and shell runtime only; do not move simulation hot paths into Unity lifecycle methods. |
| [Microsoft Learn: Unit testing best practices for .NET](https://learn.microsoft.com/dotnet/core/testing/unit-testing-best-practices) | Fast, isolated, repeatable, self-checking tests; clear test naming and minimal inputs. | Focused module/integration tests should prove ownership, event flow, no-touch boundaries, and deterministic behavior rather than giant brittle snapshots. |
| [Microsoft Learn: Reduce memory allocations using C# features](https://learn.microsoft.com/dotnet/csharp/advanced-topics/performance/) | Measure first, optimize hot paths, reduce allocations/copies only where repeated execution proves cost. | Scheduler, event drain, projection builders, migrations, and long-run diagnostics should name cardinality and allocation risk before adding broad caches or low-level rewrites. |
| [Microsoft Learn: dotnet-counters](https://learn.microsoft.com/dotnet/core/diagnostics/dotnet-counters) | First-level runtime performance monitoring for CPU, GC, allocation rate, exceptions, and memory. | Use counters for preview/long-run health investigations when runtime behavior changes; do not turn doc-only or skill-only edits into fake performance validation. |
| [Microsoft Learn: High-performance logging in .NET](https://learn.microsoft.com/dotnet/core/extensions/logging/high-performance-logging) | Source-generated logging avoids boxing, temporary allocations, and runtime template parsing. | Diagnostics in hot authority paths should be module-attributed and low-allocation; player-facing receipts remain projections, not logs. |
| [Microsoft Learn: Selecting a collection class](https://learn.microsoft.com/dotnet/standard/collections/selecting-a-collection-class) | Collection choice depends on access pattern, ordering, memory, lookup, and concurrency needs. | Use lists/arrays for stable ordered passes, dictionaries for keyed lookup, queues for bounded drains, and never let unordered traversal decide authority outcomes. |
| [Unity Manual: CPU Usage Profiler module introduction](https://docs.unity3d.com/Manual/profiler-cpu-introduction.html) | Unity profiling finds script, rendering, animation, GC allocation, and thread timing costs in the actual player/editor target. | Unity shell performance claims need profiling evidence when implementation changes; profiler data cannot justify moving authority into MonoBehaviours. |
| [Unity Manual: Managed memory introduction](https://docs.unity3d.com/Manual/performance-managed-memory-introduction.html) | Unity managed heap allocation, GC samples, fragmentation, and collection pauses affect frame behavior. | Avoid per-frame allocations in shell bindings and rows; this remains presentation performance work and does not move simulation authority into Unity. |
| [Unity Learn: Object pooling in Unity 6](https://learn.unity.com/tutorial/use-object-pooling-to-boost-performance-of-c-scripts-in-unity) | Pool frequently created/destroyed GameObjects or reusable C# entities after profiling shows churn. | Pool notice rows, markers, and shell objects only in Unity/presentation; do not pool or cache mutable simulation state as authority. |
| [Unity: UI optimization tips](https://unity.com/how-to/unity-ui-optimization-tips) | Canvas splitting, GraphicRaycaster limits, layout-group caution, UI object pooling, and efficient hiding. | Great hall, desk, notice tray, and public-life surfaces should use precomputed ViewModels and stable canvases; UI must not scan histories or recompute rules every frame. |
| [WCAG 2.2](https://www.w3.org/TR/WCAG22/) | Perceivable, operable, understandable, robust interface criteria and testable success criteria. | Shell surfaces should preserve contrast, focus/read order, non-color cues, labels, and understandable status changes even when rendered as in-world objects. |
| [Xbox Accessibility Guideline 102: Contrast](https://learn.microsoft.com/en-us/gaming/accessibility/xbox-accessibility-guidelines/102) | Game UI contrast for text, controls, symbols, HUD/map-like elements. | Notices, route markers, seals, conflict tokens, and sandbox nodes need contrast and non-color redundancy, not just atmospheric art direction. |
| [Xbox Accessibility Guideline 106: Screen narration](https://learn.microsoft.com/en-us/gaming/accessibility/xbox-accessibility-guidelines/106) | Screen narration, labels, roles, values, context/focus/status announcements. | ViewModels/adapters should carry semantic labels and state summaries for object-anchored shell surfaces; Unity-only visuals must not be the only source of meaning. |

## Skill Matrix

| Skill | Primary Use | Repo Fact To Check | External Norm Fit | Must Not Do | Validation Surface |
| --- | --- | --- | --- | --- | --- |
| `zongzu-architecture-boundaries` | Module ownership, scheduler flow, command/query/event seams, save/version contracts, extension boundaries, hot-path ownership. | `ModuleRunner<TState>`, `MonthlyScheduler`, `GameSimulation`, module `PublishedEvents` / `ConsumedEvents`, boundary docs, projection contexts, v32-v492 event-contract, owner-lane, court-policy, SocialMemory, PublicLife, social-mobility/fidelity, regime readback, personnel-flow readiness/gate/future-lane preflight, commoner/social-position readback/source keys/scale-budget, household mobility dynamics explanation/closeout/preflight/preflight-closeout, and closeout/preflight-audit evidence. | .NET modular testability, explicit dependency discipline, measured hot-path optimization, low-allocation diagnostics. | Add a giant world manager, Application second rule layer, runtime plugin marketplace, foreign state mutation, UI/Unity rule path, cooldown/docket/suggested-action/receipt/receipt-docket/public-life-receipt/social-mobility/regime/personnel/future-lane/status/household-mobility ledger, global person scanner, or global cache that changes determinism. | Architecture/integration tests, event-contract tests, v35-v100 owner-lane tests/readback tests, v101-v108 closeout guard, v109-v492 court-policy/social-mobility/regime/personnel-flow/social-position/household-mobility guards, `git diff --check`, focused compile/test lane, counter/profiler evidence when performance behavior changes. |
| `zongzu-game-design` | Living-world loop, bounded player leverage, monthly review, actor autonomy, MVP/post-MVP shape, fidelity/scale budget. | `PRODUCT_SCOPE.md`, `SIMULATION.md`, `SIMULATION_FIDELITY_MODEL.md`, `PLAYER_SCOPE.md`, implementation of command receipts/projections, v35-v492 thin handoff/readback/preflight/closeout, narrow court-policy rule-density, social-mobility/fidelity, regime readback, personnel-flow readiness/gate/future-lane preflight, social-position/source-key/scale-budget passes, household mobility dynamics explanation/closeout/preflight/preflight-closeout, and current no-ledger/no-schema discipline. | Game-system proposal discipline: owner, cadence, state, event, projection, player response, tests, cardinality/fanout budget. | Build an event-pool game, fixed route tree, player-as-god interface, detached tactics layer, full-map high-fidelity simulation by default, treat every future contract as immediate gameplay, mistake thin-chain closeout for full-chain completion, turn Chain 8 into a full court engine too early, or treat social mobility/regime/personnel-flow/social-position/household-mobility readbacks as direct control systems or new algorithms. | Acceptance tests, command-resolution tests, projection/receipt tests, no-touch tests, closeout/debt distinction tests, court-policy local response / SocialMemory echo / public follow-up / docket guard / suggested action / suggested receipt / receipt-docket / public-life receipt echo guard tests, social-mobility scale/fidelity/influence tests, regime readback tests, personnel-flow readiness/gate/future-lane guard tests, social-position source/scale-budget guard tests, household mobility dynamics explanation/closeout/preflight/preflight-closeout tests, long-run diagnostics when balance or scale changes. |
| `zongzu-pressure-chain` | Cross-module pressure propagation, especially Renzong thin/full chains, public-life/order/social-memory closure, and owner-lane handoffs. | `RENZONG_THIN_CHAIN_TOPOLOGY_INDEX.md`, module event metadata, scheduler drain, off-scope tests, public-life/order/court-policy/social-mobility/regime/personnel-flow/social-position/household-mobility v3-v492 evidence, v32-v34 event-contract diagnostics, v35-v100 owner-lane handoff/readback, v101-v108 closeout audit, v109-v204 Chain 8 rule-density branch, v213-v292 mobility/fidelity branch, v253-v268 regime readback, v293-v380 personnel-flow readiness/gate/future-lane preflight, v381-v452 social-position/source-key/scale-budget preflight, v453-v460 household mobility dynamics explanation, v461-v468 closeout, v469-v476 owner-lane preflight, and v485-v492 preflight closeout. | Traceable cause-effect design, minimal focused tests, explicit fanout/allocation proof. | Call a chain complete from docs alone, parse summaries as authority, fan out global pressure without locus/allocation, describe refusal/response residue as an event-pool design, mistake diagnostic classification for runtime propagation, treat closeout/preflight audit as full-chain rule density, or turn court-policy/social-mobility/regime/personnel-flow/social-position/household-mobility readbacks into new result engines. | Integration tests proving propagation, no-touch entities, metadata, projection receipts, SocialMemory-owned residue when in scope, classified event debt, owner/evidence backlinks, owner-lane readback proof, closeout/preflight audit guard, v109-v492 branch proof, and bounded chain complexity. |
| `zongzu-ancient-china` | Historical grounding, Northern Song/Renzong semantics, anti-anachronism, history-to-rule translation. | Historical claims in docs/content/code, module owner for each historical pressure carrier, fidelity ring for each historical scale, canal/water-network language when route pressure is involved. | Source calibration, confidence bands, scale-aware abstraction. | Turn history into fixed cutscenes, generic dynasty flavor, UI-owned historical authority, universal canal formulas for every county, or over-modeled named-person density everywhere. | Source notes, content review, chain/module docs, tests if history changes rules. |
| `zongzu-content-authoring` | Chinese copy, descriptors, authored configs, projection wording, content packs, localization-facing text. | `content/`, docs, projection copy, ViewModel labels, `WRITING_AND_COPY_GUIDELINES.md`, generated asset manifests, public-life/order and canal/route/order wording. | Content governance, localization hygiene, UTF-8 preservation, cardinality/provenance control. | Let prose become authority, replace structured rules with narrative triggers, corrupt Chinese text, or grow content into an unbounded event pool. | Encoding review, parser/schema tests when content is read by code, focused copy review for text-only changes, manifest/provenance review for generated assets. |
| `zongzu-ui-shell` | Spatialized shell design: great hall, ancestral hall, desk sandbox, notice tray, conflict/campaign surfaces. | `UI_AND_PRESENTATION.md`, `VISUAL_FORM_AND_INTERACTION.md`, read-model bundle shape, projection contexts such as desk/hall selectors, v35-v492 route/market/order/family/office/force/warfare/court/readback/public-reading/follow-up/docket-guard/suggested-action/suggested-receipt/receipt-docket/public-life-receipt/social-mobility/regime/personnel-flow/social-position/household-mobility surfaces when projected. | WCAG/XAG contrast, focus/read order, non-color cues, semantic labels, Unity UI performance discipline. | Turn UI into dashboard/card wall/spreadsheet, resolve rules in UI, scan long histories per frame, compute canal exposure/sponsor targeting/Office closure/Family guarantee/relief/campaign/aftermath/court-policy/public-reading/follow-up/docket/suggested-action/suggested-receipt/receipt-docket/public-life-receipt/social-mobility/regime/personnel-flow/social-position/household-mobility outcomes or future eligibility/preflight-closeout authority in UI, or hide essential meaning in visuals only. | Presentation adapter tests, ViewModel review, screenshot/manual checks when UI is runnable, profiling only when implementation/performance changes. |
| `zongzu-unity-shell` | Unity host implementation, scenes, prefabs, MonoBehaviours, bindings, ViewModels, presentation tests. | `unity/Zongzu.UnityShell`, `src/Zongzu.Presentation.Unity`, `tests/Zongzu.Presentation.Unity.Tests`, Unity `6000.3.13f1`, projected route/market/order/family/office/force/warfare/court/public-reading/follow-up/docket-guard/suggested-action/suggested-receipt/receipt-docket/public-life-receipt/social-mobility/regime/personnel-flow/social-position/household-mobility readback when ViewModels expose it. | Unity project organization, `.meta` discipline, asmdef/dependency hygiene, Profiler/ObjectPool/UI Canvas performance practices. | Let MonoBehaviours own simulation rules, bypass command surface, invent Unity-only facts, hide simulation cadence in `Update`, inspect event metadata from Unity, compute owner-lane/campaign/court/public-reading/follow-up/docket/suggested-action/suggested-receipt/receipt-docket/public-life-receipt/social-mobility/regime/personnel-flow/social-position/household-mobility outcomes, infer SocialMemory residue meaning, or pool authoritative state. | `dotnet test tests/Zongzu.Presentation.Unity.Tests/Zongzu.Presentation.Unity.Tests.csproj --no-restore`; Unity profiler/editor/play-mode checks only when configured and relevant. |
| `zongzu-save-and-schema` | Persisted module state, root/module schema versions, migrations, manifests, feature-pack save membership. | `SCHEMA_NAMESPACE_RULES.md`, `DATA_SCHEMA.md`, module `ModuleSchemaVersion`, save/load tests; currently `TradeAndIndustry` schema `4`, `WorldSettlements` schema `8`, `OrderAndBanditry` schema `9`, `OfficeAndCareer` schema `7`, `FamilyCore` schema `8`, `PopulationAndHouseholds` schema `3`, `SocialMemoryAndRelations` schema `3`, `ConflictAndForce` schema `4`, and `WarfareCampaign` schema `4`; v35-v492 add no schema unless a future owner state pass says otherwise. | Versioned data, migration discipline, bounded load/migration cost. | Bump schema casually, store UI-only facts as authority, persist denormalized projection caches without contract, mutate foreign state during migration, treat runtime event metadata/readback fields as persisted state, or create a canal/owner-lane/relief/sponsor-lane/Office/Family/Force/Warfare/Court/policy-public-reading/follow-up/docket/suggested-action/suggested-receipt/receipt-docket/public-life-receipt/social-mobility/regime/personnel/future-lane/status/household-mobility/cooldown/preflight-closeout ledger without migration. | Persistence round-trip, migration tests, manifest tests, determinism checks if backfill changes behavior, load-size/perf diagnostics when migration shape grows. |
| `zongzu-simulation-validation` | Determinism, replay, event flow, scheduler cadence, long-run pressure health, acceptance proof, performance diagnostics. | `MonthlyScheduler`, `ModuleRunner<TState>` metadata, ten-year diagnostics, integration tests, replay hash, event-contract classification/gates/backlinks, v35-v100 owner-lane proof, v101-v108 closeout audit, v109-v492 court-policy/social-mobility/regime/personnel-flow/social-position/household-mobility proof, counters when needed. | .NET test best practices: repeatable, isolated, self-checking evidence; `dotnet-counters` for first-level runtime metrics. | Treat high pressure as automatically wrong, tune blindly, ignore unconsumed/never-emitted events, rely on diagnostic text as runtime authority, claim thin-chain closeout as full-chain proof, or claim performance improvement without baseline. | Focused tests first, then `dotnet test Zongzu.sln --no-restore` when runtime behavior changes; event-contract no-unclassified gate and owner/evidence readback; closeout/debt distinction guard; court-policy local-response/SocialMemory/public-reading/follow-up/docket/suggested-action/suggested-receipt/receipt-docket/public-life-receipt guard proof; social-mobility/regime/personnel-flow/social-position/household-mobility guard/preflight/preflight-closeout proof; optional counters/profiling for long-run or allocation-sensitive changes; record no runtime/performance validation for skill-only/doc-only edits. |

## Default Sequencing

Use skills as one connected pass rather than isolated summaries:

| Task Shape | Recommended Skill Order |
| --- | --- |
| Broad codebase alignment | `zongzu-architecture-boundaries` -> `zongzu-game-design` -> `zongzu-pressure-chain` -> `zongzu-ui-shell` -> `zongzu-simulation-validation`; add `zongzu-ancient-china` for Renzong/historical semantics and `zongzu-save-and-schema` for persisted state. |
| Renzong / historical pressure | `zongzu-ancient-china` -> `zongzu-game-design` -> `zongzu-pressure-chain` -> `zongzu-architecture-boundaries` -> `zongzu-simulation-validation`. |
| Read-model / notification helper | `zongzu-architecture-boundaries` -> `zongzu-ui-shell`; add `zongzu-simulation-validation` only if behavior/tests need proof. |
| Unity presentation implementation | `zongzu-ui-shell` -> `zongzu-unity-shell` -> `zongzu-architecture-boundaries`; add accessibility calibration from WCAG/XAG. |
| Save or schema work | `zongzu-save-and-schema` -> `zongzu-architecture-boundaries` -> `zongzu-simulation-validation`. |
| Chinese copy or content | `zongzu-content-authoring` -> `zongzu-ancient-china` -> `zongzu-ui-shell` when player-facing surfaces are involved. |

## Skill Usage Modes

Use the smallest mode that proves the claim:

| Mode | Use When | Expected Output |
| --- | --- | --- |
| Fast Lane | The task is a narrow code, doc, copy, projection, adapter, or validation check. | Name the owner/boundary, state no-save or no-runtime impact when true, run the smallest relevant check, and stop. |
| Whole-Skill Pass | The prompt is broad, crosses modules, changes cadence/state/schema, or explicitly names a Zongzu skill chip. | Connect upstream pressure, owning modules, player surface, validation, docs, and future boundary. |
| External Calibration Pass | The user asks for outside correction, performance/algorithm structure, historical source grounding, Unity practice, or .NET guidance. | Use first-party sources where possible, translate them into Zongzu constraints, and do not override product truth. |

## External Norm Translation Rules

- Use Unity assembly definitions and folder standards to keep shell implementation modular, not to make Unity the simulation host.
- Use accessibility standards to strengthen shell readability, narration, contrast, focus order, and semantic labels; do not flatten the shell into a generic web dashboard.
- Use .NET test best practices to keep tests readable and behavior-focused; do not replace cross-module evidence with snapshot mass.
- Use .NET performance guidance as a measured hot-path discipline: name the path, cardinality, allocation risk, and baseline before adding caches, pools, `Span`/`Memory`, or ref-style rewrites.
- Use .NET collection guidance to choose data structures by access pattern and ordering requirement; stable deterministic iteration beats clever containers in authority code.
- Use `dotnet-counters` / runtime metrics for preview-run and long-run health investigations when CPU, GC, allocation rate, or exception rate is the actual risk.
- Use source-generated or otherwise low-allocation logging only for diagnostics; logs are not player receipts and never become rule input.
- Use Unity Profiler, managed-memory guidance, object pooling, Canvas splitting, and UI hot-path advice inside the Unity shell only; simulation authority remains pure C# and projection-driven.
- Use source calibration for history, but translate it into pressure carriers, module state, events, projections, and bounded player leverage.
- Treat external references as evidence for "how to implement responsibly," not as permission to change Zongzu's product constraints.

## Performance / Scheduling / Algorithm Rules

- Every non-trivial system proposal should name its expected cardinality: settlements, clans, households, people, events, notices, routes, or UI objects touched per day/month/frame.
- Scheduler work should remain ordered, bounded, and deterministic. New drains, recovery passes, or recurring-demand models need a cap, watermark, or explicit cadence.
- Data-structure changes should name access pattern, ordering, memory/payload effect, and whether lookup speed is worth any extra index or cache.
- Pressure chains must prove fanout: source -> locus -> affected owners -> no-touch boundary. "All settlements react" is a missing allocation rule unless deliberately specified and tested.
- Event-contract diagnostics may classify debt, gate unclassified entries, and show owner/evidence backlinks; they may not become scheduler decisions, runtime state, command eligibility, or player receipts.
- Owner-lane event consumers should name metadata keys, query seams, deterministic sorting/cap, touched/off-scope proof, and whether they adjust existing state or require a schema plan.
- Projection and shell code may build one-pass indexes over already-built read models. They may not re-scan long histories in every adapter, frame, or Unity binding.
- Performance caches are allowed only when their authority, invalidation point, determinism impact, and save/schema status are explicit.
- Unity shell performance work should watch GC allocations, canvas/layout churn, object lifetime, and row/marker counts; pooling and virtualization are presentation tools, not rule systems.
- Full fidelity belongs near the player's focus ring or current pressure hit. Distant society should stay alive through summarized pressure until visibility or leverage justifies promotion.
- Migration/load paths are hot when old saves are opened; schema growth should consider payload size, deterministic defaults, and migration test cost.
- For doc-only, skill-only, or wording-only changes, record `no runtime/performance validation required` rather than running unrelated tests.

## Red Flags

- A skill answer stops after one narrow definition when the prompt implies a whole-system pass.
- A UI suggestion invents a player-facing fact not present in a projection or ViewModel.
- A pressure-chain suggestion lacks an owner module, settlement/locus, off-scope boundary, event metadata, and a no-touch test.
- A Unity suggestion depends on Editor MCP or scene automation that is not configured.
- A historical suggestion uses named people or reforms as direct gameplay triggers.
- A save/schema suggestion treats read-only helper methods as persisted state.
- A validation suggestion calls `dotnet test` enough proof when the real risk is event topology or long-run pressure saturation.
- A performance suggestion adds a cache, pool, or index without naming authority, invalidation, determinism, cardinality, and save/schema impact.
- A shell optimization moves rule resolution into Unity or scans projection history from `Update`.
- A historical or content expansion creates dense named actors everywhere instead of using focus rings and pressure-driven promotion.

## Current Impact

This document is governance/orchestration only. It has no runtime, save/schema, determinism, or Unity asset impact.

## V38-V45 Office/Yamen Readback Spine - 2026-04-26

- This pass uses the Zongzu skills as one connected pass: game design, architecture boundaries, pressure-chain, content authoring, save/schema, simulation validation, UI shell, Unity shell, and ancient-China grounding.
- V38-V45 closes after v37 `OfficeAndCareer.PolicyImplemented`: public-life and governance readbacks show county-yamen implementation state; office affordances point to press-document / redirect-report / cool-observe next steps; later monthly SocialMemory can write office/yamen residue from structured jurisdiction snapshots; Unity copies projected fields only.
- The work remains a rule-driven command / aftermath / social-memory / readback loop. `DomainEvent` is still a deterministic fact propagation tool, not an event-pool design body.
- Save/schema result: no persisted state, schema bump, migration, policy ledger, yamen workflow state, owner-lane ledger, cooldown ledger, household target field, or `PersonRegistry` expansion. `OfficeAndCareer` stays schema `7`; `PublicLifeAndRumor` stays schema `4`; `SocialMemoryAndRelations` stays schema `3`.
- Ordinary household response remains a low-power local response surface, not a universal repair line. County-yamen, document drag, and clerk delay return to `OfficeAndCareer`; public heat stays `PublicLifeAndRumor`; durable shame/fear/favor/grudge/obligation remains `SocialMemoryAndRelations`.

## Skill Alignment Through V60 - 2026-04-26

- The local Codex skill pack and repo-tracked `.github/skills` pack should carry the same v60 anchors: v31 merge cleanup, v32-v34 event-contract diagnostics, v35 canal-window Trade/Order handoff, v36 household-burden Family handoff, v37-v45 Office/yamen implementation/readback, v46-v52 Office-lane closure, and v53-v60 Family-lane closure readback.
- External calibration is intentionally narrow: Microsoft Learn .NET testing/diagnostics/logging, Unity assembly/metadata/profiler/pooling/UI guidance, WCAG 2.2, and Xbox Accessibility Guidelines calibrate proof quality, hot-path discipline, Unity organization, and readability.
- Skill guidance now treats performance, scheduling, and algorithm choices as first-class boundary questions: name hot path, cardinality, cap/watermark/cadence, deterministic tie-break, cache invalidation, save/schema impact, and validation lane before adding broad complexity.
- This alignment is docs/skill governance only. It has no runtime behavior, save/schema, migration, deterministic replay, or Unity asset impact; no runtime/performance validation is required.

## Skill Alignment Through V76 - 2026-04-26

- The whole-skill pass now also anchors v61-v68 Family relief choice and v69-v76 Force/Campaign/Regime owner-lane readback closure. The intended sequencing is architecture boundaries -> game design -> pressure-chain -> content authoring -> save/schema -> simulation validation -> UI shell -> ancient-China grounding.
- V69-V76 is projection/readback guidance only: `军务承接入口`, `Force承接读回`, `战后后账读回`, `军务后手收口读回`, `军务余味续接读回`, `军务闭环防回压`, `不是普通家户硬扛`, and `不是把军务后账误读成县门/Order后账` are projected strings over existing structured read models.
- Ownership remains skill-aligned: `ConflictAndForce` owns force posture/readiness, `WarfareCampaign` owns campaign boards/directives/aftermath, `OfficeAndCareer` owns office coordination/regime authority, `PopulationAndHouseholds` owns ordinary local household response only, and `SocialMemoryAndRelations` owns durable residue in its own cadence.
- Save/schema result: v69-v76 adds no persisted state, schema bump, migration, new ledger, household target field, manager/controller layer, `PersonRegistry` expansion, Unity authority, or Application-owned rule layer. Validation is code/test evidence, not runtime performance work.
- V77-V84 keeps that same skill sequence but turns existing WarfareCampaign directive commands into clearer owner-lane choice readback: `军令选择读回`, `WarfareCampaign拥有军令`, and `军务选择不是县门文移代打` are projected/receipt wording over existing directive state. Save/schema result: no persisted state, schema bump, migration, directive ledger, owner-lane ledger, household target field, manager/controller layer, `PersonRegistry` expansion, Unity authority, or Application-owned rule layer.

## Skill Alignment Through V108 - 2026-04-26

- V101-V108 is a whole-skill closeout audit, not a new feature. The skill sequence is architecture boundaries -> game design -> pressure-chain -> save/schema -> simulation validation -> UI shell.
- The audit locks the meaning of "thin-chain complete": v3-v100 now prove thin topology, module ownership, structured metadata/query seams, bounded scheduler drain or delayed monthly residue, no-touch/off-scope boundaries, player-facing projection/readback, UI/Unity copy-only display, no-summary-parsing guards, and no-save/no-schema evidence.
- The audit also locks the opposite boundary: the full-chain work is still future rule density. Taxes, famine, disaster relief, office factions, thick court process, campaign logistics, regime recognition, canal politics, durable residue depth, and long-run recovery/decay are not completed by the thin-chain closeout.
- Validation expectation: no runtime/performance validation is required for the docs-only audit itself beyond focused architecture guard, build, `git diff --check`, and full solution test. Any future rule-density pass must open its own ExecPlan and choose a fresh validation lane.

## Skill Pack Alignment Through V108 - 2026-04-27

- The repo-tracked `.github/skills` pack now carries v108 anchors across all nine Zongzu skills and the local `microsoft-code-reference` helper.
- The skill pack treats v101-v108 as thin-chain closeout evidence only. Future full-chain work must name owner module, state, cadence, fanout, command/projection path, save/schema impact, and validation lane before adding rule density.
- Performance, scheduling, and algorithm guidance remains evidence-first: name hot path, expected touched counts, deterministic ordering, cap/watermark/cadence, cache invalidation, save/schema impact, and counter/profiler/test lane before claiming optimization.
- This alignment is docs/skill governance only. It has no runtime behavior, save/schema, migration, deterministic replay, Unity asset, or content-pack impact; no runtime/performance validation is required.

## Skill Pack Alignment Through V148 - 2026-04-27

- The repo-tracked `.github/skills` pack now carries v148 anchors across the nine Zongzu skills and the local `microsoft-code-reference` helper.
- The skill pack treats v109-v148 as the first narrow Chain 8 rule-density branch after thin-chain closeout: policy process texture, Office-owned local response, delayed SocialMemory residue, memory-pressure next-window readback, and public-reading echo.
- The same hard boundaries remain: no schema bump, no migration, no court module, no policy/public-reading ledger, no Application rule layer, no UI/Unity authority, no summary/prose parsing, and no `PersonRegistry` expansion.
- Performance and scheduling guidance remains evidence-first: court-policy echo work is projection/readback and focused-test evidence; future hot-path changes must name workload, touched counts, deterministic ordering, cap/watermark/cadence, save/schema impact, and counter/profiler/test lane before claiming optimization.

## Skill Pack Alignment Through V196 - 2026-04-27

- The repo-tracked `.github/skills` pack now carries v196 anchors across the nine Zongzu skills and the local `microsoft-code-reference` helper.
- The skill pack treats v149-v196 as projection/readback guard layering on the same narrow Chain 8 first rule-density branch: public follow-up cue, docket guard, suggested-action guard, suggested-receipt guard, receipt-docket consistency guard, and public-life receipt echo guard.
- The hard boundaries remain unchanged: no schema bump, no migration, no Court module, no cooldown/docket/suggested-action/suggested-receipt/receipt-docket/public-life-receipt ledger, no Application rule/ranking layer, no UI/Unity authority, no prose parsing, and no `PersonRegistry` expansion.
- Performance and scheduling guidance remains evidence-first: these guard layers are projection/readback proof work; future algorithm or runtime changes must name hot path, touched counts, deterministic ordering, cap/watermark/cadence, cache invalidation, save/schema impact, and counter/profiler/test lane before claiming optimization.

## Skill Pack Alignment Through V300 - 2026-04-28

- The repo-tracked `.github/skills` pack now carries current mainline v300 anchors across all nine Zongzu skills and the local `microsoft-code-reference` helper.
- The skill pack treats v197-v204 as Chain 8 closeout, v213-v292 as the first social mobility / fidelity-ring / influence-readback substrate, v253-v268 as Chain 9 regime-legitimacy readback and closeout, and v293-v300 as personnel-command preflight guardrails. None of these entries claims a full court engine, full society engine, full regime engine, or direct personnel-command system.
- The same hard boundaries remain: no schema bump, no migration, no Court module, no mobility/personnel/regime ledger, no `PersonRegistry` domain expansion beyond the existing fidelity ring, no Application rule/ranking layer, no UI/Unity authority, no prose parsing, and no event-pool loop.
- Performance, scheduling, and algorithm guidance remains evidence-first: future runtime optimization must name the hot path, touched counts, deterministic ordering, cap/watermark/cadence, cache invalidation, save/schema impact, and counter/profiler/test lane; this skill-pack alignment itself is governance-only with no runtime/performance validation required.

## Skill Pack Alignment Through V380 - 2026-04-28

- The repo-tracked `.github/skills` pack now carries current mainline v380 anchors across all nine Zongzu skills and the local `microsoft-code-reference` helper.
- The skill pack treats v301-v324 as the first personnel-flow command-readiness layer, v325-v356 as owner-lane gate / desk echo / containment, and v357-v380 as future Family/Office/Warfare lane preflight visibility plus closeout. It does not treat any of those passes as direct movement, transfer, summon, assignment, office-service, kin-transfer, campaign-manpower, or migration-economy command implementation.
- The hard boundaries remain: no schema bump, no migration, no command/movement/personnel/future-lane ledger, no `PersonRegistry` domain expansion, no Application movement resolver, no UI/Unity authority, no prose parsing, no event-pool loop, and no hidden owner-lane unlock from readback text.
- Performance, scheduling, and algorithm guidance remains evidence-first: future personnel-flow rule work must name owner module, accepted command, target scope, hot path, expected cardinality, deterministic order/cap, cadence, schema impact, no-touch boundary, projection fields, and validation lane before implementation.

## Skill Pack Alignment Through V460 - 2026-04-28

- The repo-tracked `.github/skills` pack now carries current mainline v460 anchors across all nine Zongzu skills and the local `microsoft-code-reference` helper.
- The skill pack treats v381-v396 as commoner/social-position preflight plus runtime readback, v397-v404 as source-key provenance, v405-v436 as source/scale-budget/regional-summary closeout, v437-v444 as commoner status owner-lane preflight, v445-v452 as fidelity scale-budget preflight, and v453-v460 as household mobility dynamics explanation.
- The hard boundaries remain: no schema bump, no migration, no commoner/status/social-position/household-mobility ledger, no `PersonRegistry` domain expansion, no Application rank or mobility resolver, no global person scan, no UI/Unity authority, no prose parsing, no event-pool loop, no class ladder, and no hidden fidelity promotion from readback text.
- Performance, scheduling, algorithm, topology, and feature-pack guidance is now explicit: future detail promotion or status-depth work must name owner module, target scope, hot path, touched counts, complexity, deterministic cap/order, cadence, schema impact, projection fields, no-touch boundary, and validation lane before implementation.

## Skill Pack Alignment Through V468 - 2026-04-29

- The repo-tracked `.github/skills` pack now carries current mainline v468 anchors across all nine Zongzu skills and the local `microsoft-code-reference` helper.
- The skill pack treats v461-v468 as closeout governance for v453-v460 household mobility dynamics explanation. It does not treat the closeout as a migration economy, route-history model, class/status engine, direct movement command, durable residue path, selector, schema change, or `PersonRegistry` expansion.
- The hard boundaries remain: no schema bump, no migration, no household-mobility/movement/route-history/status ledger, no Application mobility resolver, no global person scan, no UI/Unity authority, no prose parsing, no event-pool loop, and no hidden fidelity promotion or per-person distant simulation from readback text.
- Performance, scheduling, algorithm, topology, and feature-pack guidance remains evidence-first: future household mobility depth must name owner module, cadence, target scope, hot path, touched counts, deterministic cap/order, schema impact, projection fields, no-touch boundary, and validation lane before implementation.
- External calibration for this pass used official Microsoft Learn, Unity Manual / Unity guidance, W3C WCAG 2.2, and Xbox Accessibility Guidelines only to reinforce already-present skill discipline: focused tests, first-level counters, source-generated / low-allocation diagnostics, collection choice by access pattern, profiler-first Unity shell work, bounded `GC.Alloc`, assembly boundaries, contrast, non-color cues, and screen narration. These sources do not override repo ownership facts or authorize runtime features.
- The v468 skill pack now explicitly carries additional future-facing dimensions: scheduler drain/cap proof, topology node/edge/fanout declarations, feature-pack manifest/save membership, no runtime plugin marketplace, near-detail/far-summary scale policy, player-facing levers/tradeoffs/counterpressure, content stable IDs/provenance/cardinality, historical uncertainty/regional variation, Unity asmdef/pooling/canvas/profiler guidance, and accessibility/readability requirements.

## Skill Pack Alignment Through V476 - 2026-04-29

- The repo-tracked `.github/skills` pack now carries current mainline v476 anchors across all nine Zongzu skills and the local `microsoft-code-reference` helper.
- The skill pack treats v469-v476 as household mobility owner-lane preflight, not a runtime feature. Future household mobility depth should start from `PopulationAndHouseholds` unless a later ExecPlan proves another owner lane.
- Future skill use after v476 must require owner state, cadence, target scope, hot path, touched counts, deterministic cap/order, no-touch boundary, schema impact, projection fields, and validation before adding household movement commands, route history, status drift, durable residue, selector watermarks, fidelity promotion, or distant per-person simulation.
- The hard boundaries remain: no schema bump, no migration, no household-mobility/movement/route-history/status ledger, no Application mobility resolver, no global person scan, no UI/Unity authority, no prose parsing, no event-pool loop, and no hidden fidelity promotion or per-person distant simulation from readback/preflight text.
- This alignment is docs/skill governance only. It has no runtime behavior, save/schema, migration, deterministic replay, Unity asset, or content-pack impact; no runtime/performance validation is required.

## Household Mobility Preflight Closeout Through V492 - 2026-04-29

- The active whole-skill pass treats v485-v492 as closeout governance for v469-v476 household mobility owner-lane preflight, not a runtime feature.
- The closeout keeps household mobility depth behind a future owner-lane ExecPlan with owner state, cadence, target scope, hot path, touched counts, deterministic cap/order, no-touch boundary, schema impact, projection fields, and validation.
- Future skill use after v492 must still prevent household movement commands, route-history state, migration economy, selector watermarks, durable movement residue, status/class drift, fidelity promotion, distant per-person simulation, Application mobility resolvers, UI/Unity authority, and prose parsing until a real owner-laned rule lands.
- Validation expectation: the v485-v492 guard proves no production rule, schema, ledger, parser, Application/UI/Unity authority, mobility engine, route-history model, direct movement command, or `PersonRegistry` expansion.

## Household Mobility Runtime Rules-Data Readiness Through V508 - 2026-04-30

- The active whole-skill pass treats v501-v508 as first runtime rule readiness and hardcoded extraction mapping, not a runtime household movement feature.
- `zongzu-game-design` keeps the future rule monthly-first, bounded, and near-detail/far-summary; `zongzu-architecture-boundaries` keeps ownership in `PopulationAndHouseholds`; `zongzu-save-and-schema` records no persisted change; `zongzu-simulation-validation` scopes proof to architecture/docs validation plus build/full tests; `zongzu-pressure-chain` keeps fanout/no-touch requirements explicit; `zongzu-content-authoring` frames future authored rules-data as owner-consumed validated data, not prose authority; `zongzu-ancient-china` keeps regional/era assumptions as calibration bands; `zongzu-ui-shell` / `zongzu-unity-shell` keep shell surfaces copy-only.
- Future skill use after v508 may define a rules-data contract only if it remains owner-consumed, deterministic, validated, defaulted/fallback-capable, and not a runtime plugin marketplace.
- Validation expectation: the v501-v508 guard proves the docs name existing `PopulationAndHouseholds` carriers, monthly cadence, target/no-touch scope, deterministic fanout cap/order requirements, hardcoded extraction categories, no schema/migration impact, no loader, no prose parsing, no Application/UI/Unity authority, no `PersonRegistry` expansion, and no movement implementation.

## Household Mobility Rules-Data Contract Preflight Through V516 - 2026-04-30

- The active whole-skill pass treats v509-v516 as rules-data contract and validator preflight only, not a runtime config implementation.
- `zongzu-game-design` keeps future parameters tied to pressure-carrier rules rather than direct move buttons; `zongzu-architecture-boundaries` keeps the future consumer in `PopulationAndHouseholds`; `zongzu-save-and-schema` records no persisted change; `zongzu-simulation-validation` scopes proof to architecture/docs validation plus build/full tests; `zongzu-pressure-chain` keeps deterministic fanout and tie-breaks explicit; `zongzu-content-authoring` treats authored data as validated values, not executable prose; `zongzu-ancient-china` keeps regional/era modifiers as calibration inputs; `zongzu-ui-shell` / `zongzu-unity-shell` keep presentation from reading rules-data as authority.
- Future skill use after v516 may add a skeleton only if it does not invent a runtime plugin system and either reuses a clear local pattern or remains docs/tests-only.
- Validation expectation: the v509-v516 guard proves the docs name stable ids, schema/version, deterministic ordering, default fallback, validation errors, owner-consumed use, no UI/Application authority, no arbitrary script/plugin execution, future parameter categories, no current reusable runtime rules-data pattern, no schema/migration impact, no loader/default file/validator implementation, no prose parsing, no `PersonRegistry` expansion, and no movement implementation.

## Household Mobility Default Rules-Data Skeleton Through V524 - 2026-04-30

- The active whole-skill pass treats v517-v524 as default rules-data skeleton contract only, not a default config file or validator implementation.
- `zongzu-game-design` keeps the skeleton tied to bounded pressure-carrier tuning; `zongzu-architecture-boundaries` keeps the future consumer in `PopulationAndHouseholds`; `zongzu-save-and-schema` records no persisted change and no save participation; `zongzu-simulation-validation` scopes proof to architecture/docs validation plus build/full tests; `zongzu-pressure-chain` keeps fanout caps and tie-break priorities explicit; `zongzu-content-authoring` treats the skeleton as values-only future content, not prose authority; `zongzu-ancient-china` keeps regional/era groups as calibration carriers; `zongzu-ui-shell` / `zongzu-unity-shell` keep presentation from reading skeleton fields as authority.
- Future skill use after v524 should not hardcode a loader into Application/UI/Unity. If V525-V532 extracts one rule, it must be owner-consumed, validated, behavior-equivalent by default, and schema-neutral.
- Validation expectation: the v517-v524 guard proves the docs name `ruleSetId`, `schemaVersion`, `ownerModule`, `defaultFallbackPolicy`, `parameterGroups`, `validationResult`, deterministic declaration order, future parameter groups, no current reusable runtime rules-data pattern, no `content/rules-data`, no schema/migration impact, no loader/default file/validator implementation, no prose parsing, no `PersonRegistry` expansion, and no movement implementation.

## Skill Pack Alignment Through V492 - 2026-04-29

- The repo-tracked `.github/skills` pack now carries current mainline v492 anchors across all nine Zongzu skills and the local `microsoft-code-reference` helper.
- The skill pack treats v485-v492 as household mobility preflight closeout governance, not a runtime feature. It closes the v469-v476 future-rule gate without adding movement, route history, migration economy, selector state, durable residue, status/class drift, or fidelity promotion.
- Future skill use after v492 must still require a separate owner-lane ExecPlan before any household mobility rule depth lands, including owned state, cadence, target scope, hot path, touched counts, deterministic cap/order, no-touch boundary, schema impact, projection fields, and validation.
- This alignment is docs/skill governance only. It has no runtime behavior, save/schema, migration, deterministic replay, Unity asset, or content-pack impact; no runtime/performance validation is required.

## Skill Alignment Through V180 - 2026-04-27

- The active whole-skill pass now treats v109-v180 as one narrow Chain 8 first rule-density branch, not a full court engine: process texture, local Office response, delayed SocialMemory residue, memory-pressure readback, public-reading echo, public follow-up cue, docket guard, suggested-action guard, and suggested receipt guard.
- V173-V180 stays projection/readback-only. `zongzu-architecture-boundaries` keeps receipt text out of Application authority; `zongzu-pressure-chain` keeps the path structured rather than event-pool; `zongzu-save-and-schema` records no persisted state; `zongzu-ui-shell` and `zongzu-unity-shell` require copy-only receipt fields; `zongzu-content-authoring` and `zongzu-ancient-china` keep the wording as mediated county/public-life readback rather than emperor-control UI.
- Validation expectation: focused integration proves same-month SocialMemory neutrality and off-scope non-inheritance, architecture tests guard no prose parsing/no ledger/no schema, Unity presentation tests copy receipt `ReadbackSummary`, then build, `git diff --check`, and full solution tests close the pass.

## Skill Alignment Through V196 - 2026-04-27

- The active whole-skill pass now treats v189-v196 as the public-life receipt echo parity layer inside the same Chain 8 first rule-density branch, not a full court engine: public-life command readback can repeat the anti-misread rule, but only as projected command guidance.
- V189-V196 stays projection/readback-only. `zongzu-architecture-boundaries` keeps public-life echo text out of Application authority; `zongzu-pressure-chain` keeps the path structured rather than event-pool; `zongzu-save-and-schema` records no persisted state; `zongzu-simulation-validation` scopes proof to off-scope non-inheritance and same-month SocialMemory neutrality; `zongzu-ui-shell` and `zongzu-unity-shell` require public-life command field copy-only; `zongzu-content-authoring` and `zongzu-ancient-china` keep the wording mediated by county notices, road reports, and street reading rather than emperor-control UI.
- Validation expectation: focused integration proves `公议回执回声防误读` appears only on the matching public-life command surface and aligns with the suggested receipt guard, architecture tests guard no prose parsing/no ledger/no schema, Unity presentation tests copy public-life command fields, then build, `git diff --check`, and full solution tests close the pass.

## Skill Alignment Through V204 - 2026-04-27

- The active whole-skill pass now treats v197-v204 as the Chain 8 first rule-density closeout audit over v109-v196, not a new runtime feature.
- `zongzu-architecture-boundaries` locks the owner split; `zongzu-pressure-chain` keeps the closure framed as first-layer readback rather than full court process completion; `zongzu-save-and-schema` records no persisted state; `zongzu-simulation-validation` scopes proof to architecture/docs validation; `zongzu-ui-shell` / `zongzu-unity-shell` keep the shell copy-only; `zongzu-content-authoring` and `zongzu-ancient-china` preserve the county-gate, notice, report, and public-reading carriers.
- Validation expectation: architecture closeout test proves the v109-v196 subpasses are documented together, that full-court debt remains explicit, and that no schema, migration, ledger, Court module, `PersonRegistry` expansion, Application authority, UI authority, or Unity authority is added.

## Skill Alignment Through V188 - 2026-04-27

- The active whole-skill pass now treats v181-v188 as the receipt-docket consistency layer inside the same Chain 8 first rule-density branch, not a full court engine: the docket can repeat the suggested receipt anti-misread rule, but only as projected no-loop guidance.
- V181-V188 stays projection/readback-only. `zongzu-architecture-boundaries` keeps receipt/docket text out of Application authority; `zongzu-pressure-chain` keeps the path structured rather than event-pool; `zongzu-save-and-schema` records no persisted state; `zongzu-simulation-validation` scopes proof to off-scope non-inheritance and same-month SocialMemory neutrality; `zongzu-ui-shell` and `zongzu-unity-shell` require governance/docket field copy-only; `zongzu-content-authoring` and `zongzu-ancient-china` keep the wording mediated by county documents, notices, and public reading rather than emperor-control UI.
- Validation expectation: focused integration proves `回执案牍一致防误读` appears only on the matching governance/docket surface and aligns with the suggested receipt guard, architecture tests guard no prose parsing/no ledger/no schema, Unity presentation tests copy governance/docket fields, then build, `git diff --check`, and full solution tests close the pass.

## Skill Alignment Through V116 - 2026-04-27

- V109-V116 uses the Zongzu skills as one connected pass: `zongzu-game-design` frames the first rule-density layer without turning it into a full court engine; `zongzu-architecture-boundaries` keeps owner lanes fixed; `zongzu-pressure-chain` keeps Chain 8 as structured pressure propagation / readback rather than an event-pool body; `zongzu-save-and-schema` documents no persisted state; `zongzu-simulation-validation` selects focused integration / architecture / SocialMemory / Unity proof; `zongzu-ui-shell` keeps hall, desk, office, docket, and public-life surfaces copy-only; `zongzu-content-authoring` protects Chinese readback wording; `zongzu-ancient-china` keeps the policy tone, document handoff, county-gate posture, and public interpretation historically mediated rather than emperor-button control.
- The first rule-density layer adds player-facing readback tokens `政策语气读回`, `文移指向读回`, `县门承接姿态`, `公议承压读法`, `朝廷后手仍不直写地方`, and `不是本户硬扛朝廷后账` over the existing `CourtAgendaPressureAccumulated -> PolicyWindowOpened -> PolicyImplemented -> PublicLifeAndRumor` path.
- Validation expectation: focused integration proves policy window / implementation / public interpretation; architecture tests guard no Application/UI/Unity authority drift, no summary/prose parsing, no forbidden manager/god-controller names, no `PersonRegistry` expansion, and no schema drift; SocialMemory tests guard later structured residue only; Unity presentation tests guard copy-only fields.
- Save/schema result: no persisted state, schema bump, migration, Court module, dispatch ledger, policy ledger, court-process ledger, owner-lane ledger, cooldown ledger, household target field, Application rule layer, UI authority, Unity authority, or `PersonRegistry` expansion.

## Skill Alignment Through V124 - 2026-04-27

- V117-V124 continues the same connected skill pass: `zongzu-game-design` keeps the local response bounded; `zongzu-architecture-boundaries` keeps `WorldSettlements`, `OfficeAndCareer`, `PublicLifeAndRumor`, and `SocialMemoryAndRelations` in their lanes; `zongzu-pressure-chain` keeps Chain 8 as rule-driven command / aftermath / social-memory / readback; `zongzu-save-and-schema` confirms no persisted state; `zongzu-simulation-validation` drives focused command/integration/architecture/Unity proof; `zongzu-ui-shell` keeps shell surfaces copy-only; `zongzu-content-authoring` protects the Chinese command guidance; `zongzu-ancient-china` keeps the response as county document/report mediation, not court omnipotence.
- The local response affordance layer adds player-facing tokens `政策回应入口`, `文移续接选择`, `县门轻催`, `递报改道`, `公议降温只读回`, and `不是本户硬扛朝廷后账` while reusing existing `PressCountyYamenDocument`, `RedirectRoadReport`, `PostCountyNotice`, and `DispatchRoadReport` command surfaces.
- Validation expectation: focused integration proves policy pressure opens the affordance and resolves through `OfficeAndCareer`; architecture tests guard no Application/UI/Unity authority drift, no summary/prose parsing, no forbidden manager/god-controller names, no `PersonRegistry` expansion, and no schema drift; Unity presentation tests guard copy-only command/readback fields.
- Save/schema result: no persisted state, schema bump, migration, Court module, dispatch ledger, policy ledger, court-process ledger, owner-lane ledger, cooldown ledger, household target field, Application rule layer, UI authority, Unity authority, or `PersonRegistry` expansion.

## Skill Alignment Through V140 - 2026-04-27

- V133-V140 continues the whole-skill pass: `zongzu-game-design` keeps old residue as context for the next policy-window readback rather than a new court system; `zongzu-architecture-boundaries` keeps Application projection-only; `zongzu-pressure-chain` keeps the path a command / aftermath / social-memory / readback loop; `zongzu-save-and-schema` confirms no persisted state; `zongzu-simulation-validation` scopes proof to old-residue projection and off-scope non-inheritance; `zongzu-ui-shell` and `zongzu-unity-shell` keep shell copy-only; `zongzu-content-authoring` protects `政策旧账回压读回`; `zongzu-ancient-china` keeps the readback mediated by yamen documents, county posture, and public reading.
- The memory-pressure readback layer reads existing `office.policy_local_response...` SocialMemory cause/type/weight plus current Office/PublicLife snapshots only. It must not parse memory summaries, receipt prose, public-life prose, or `DomainEvent.Summary`.
- Validation expectation: integration proves old residue appears in the later next-window projection and off-scope settlements do not inherit it; architecture tests guard no forbidden manager/ledger/schema drift; Unity presentation tests guard projected-field copy only.
- Save/schema result: no persisted state, schema bump, migration, Court module, dispatch ledger, policy ledger, court-process ledger, owner-lane ledger, cooldown ledger, memory-pressure ledger, household target field, Application rule layer, UI authority, Unity authority, or `PersonRegistry` expansion.

## Skill Alignment Through V148 - 2026-04-27

- V141-V148 continues the whole-skill pass: `zongzu-game-design` keeps public-reading echo as command/readback texture rather than a new policy result; `zongzu-architecture-boundaries` keeps Office/PublicLife/SocialMemory owner lanes fixed; `zongzu-pressure-chain` keeps Chain 8 as rule-driven command / aftermath / social-memory / readback, not an event pool; `zongzu-save-and-schema` confirms no persisted state; `zongzu-simulation-validation` scopes proof to public echo and off-scope non-inheritance; `zongzu-ui-shell` and `zongzu-unity-shell` keep public-life command fields copy-only; `zongzu-content-authoring` protects `政策公议旧读回`; `zongzu-ancient-china` keeps the readback mediated by county notices, road reports, yamen documents, and street interpretation.
- The public-reading echo layer reads existing `office.policy_local_response...` SocialMemory cause/type/weight plus current Office/PublicLife snapshots only. It must not parse memory summaries, receipt prose, public-life prose, or `DomainEvent.Summary`.
- Validation expectation: integration proves old residue appears in later governance/public-life command readbacks and off-scope settlements do not inherit it; architecture tests guard no forbidden manager/ledger/schema drift; Unity presentation tests guard projected command-field copy only.
- Save/schema result: no persisted state, schema bump, migration, Court module, dispatch ledger, policy ledger, court-process ledger, owner-lane ledger, cooldown ledger, memory-pressure ledger, public-reading ledger, household target field, Application rule layer, UI authority, Unity authority, or `PersonRegistry` expansion.

## Skill Alignment Through V156 - 2026-04-27

- V149-V156 continues the whole-skill pass: `zongzu-game-design` keeps the public follow-up cue as player-facing readback texture rather than a policy result; `zongzu-architecture-boundaries` keeps Office/PublicLife/SocialMemory owner lanes fixed; `zongzu-pressure-chain` keeps Chain 8 as rule-driven command / aftermath / social-memory / readback, not an event pool; `zongzu-save-and-schema` confirms no persisted state; `zongzu-simulation-validation` scopes proof to cue projection and off-scope non-inheritance; `zongzu-ui-shell` and `zongzu-unity-shell` keep public-life command fields copy-only; `zongzu-content-authoring` protects `政策公议后手提示`; `zongzu-ancient-china` keeps the cue mediated by county notices, road reports, yamen documents, and street interpretation.
- The public follow-up cue reads existing `office.policy_local_response...` SocialMemory cause outcome data plus current PublicLife scalars only. It must not parse memory summaries, receipt prose, public-life prose, or `DomainEvent.Summary`.
- Validation expectation: integration proves old residue appears in later governance/public-life command readbacks as `政策公议后手提示`, `Contained` outcome shows `公议轻续提示`, and off-scope settlements do not inherit it; architecture tests guard no forbidden manager/ledger/schema drift; Unity presentation tests guard projected command-field copy only.
- Save/schema result: no persisted state, schema bump, migration, Court module, dispatch ledger, policy ledger, court-process ledger, owner-lane ledger, cooldown ledger, memory-pressure ledger, public-reading ledger, public-follow-up ledger, household target field, Application rule layer, UI authority, Unity authority, or `PersonRegistry` expansion.

## Skill Alignment Through V164 - 2026-04-27

- V157-V164 continues the whole-skill pass: `zongzu-game-design` keeps the docket guard as anti-misread texture rather than a policy result; `zongzu-architecture-boundaries` keeps Office/PublicLife/SocialMemory owner lanes fixed; `zongzu-pressure-chain` keeps Chain 8 as rule-driven command / aftermath / social-memory / readback, not an event pool; `zongzu-save-and-schema` confirms no persisted state; `zongzu-simulation-validation` scopes proof to docket guard projection and off-scope non-inheritance; `zongzu-ui-shell` and `zongzu-unity-shell` keep governance/docket fields copy-only; `zongzu-content-authoring` protects `政策后手案牍防误读`; `zongzu-ancient-china` keeps the guard mediated by county notices, road reports, yamen documents, and street interpretation.
- The follow-up docket guard reads existing `office.policy_local_response...` SocialMemory cause outcome data plus current PublicLife scalars only. It must not parse memory summaries, receipt prose, public-life prose, or `DomainEvent.Summary`.
- Validation expectation: integration proves old residue appears in later governance/docket no-loop readbacks as `政策后手案牍防误读`, guidance names `不是Order后账` and `不是Office成败`, and off-scope settlements do not inherit it; architecture tests guard no forbidden manager/ledger/schema drift; Unity presentation tests guard projected governance/docket field copy only.

## Skill Alignment Through V172 - 2026-04-27

- V165-V172 continues the same whole-skill pass: `zongzu-game-design` keeps the suggested action as bounded local leverage rather than a court-control button; `zongzu-architecture-boundaries` keeps command ranking and authority outside UI/Unity; `zongzu-pressure-chain` keeps Chain 8 as rule-driven command / aftermath / social-memory / readback, not an event pool; `zongzu-save-and-schema` confirms no persisted state; `zongzu-simulation-validation` scopes proof to prompt projection and off-scope non-inheritance; `zongzu-ui-shell` and `zongzu-unity-shell` keep governance/docket text copy-only; `zongzu-content-authoring` protects `建议动作防误读`; `zongzu-ancient-china` keeps the prompt mediated by county notices, road reports, yamen documents, and street interpretation.
- The suggested action guard reads existing `office.policy_local_response...` SocialMemory cause outcome data, current PublicLife scalars, and the already-selected `PlayerCommandAffordanceSnapshot` only. It must not parse memory summaries, receipt prose, public-life prose, affordance prose, or `DomainEvent.Summary`.
- Validation expectation: integration proves old residue appears in later `SuggestedCommandPrompt` / docket guidance as `建议动作防误读`, guidance names `只承接已投影的政策公议后手`, and off-scope settlements do not inherit it; architecture tests guard no forbidden manager/ledger/schema/ranking drift; Unity presentation tests guard projected governance/docket field copy only.
- Save/schema result: no persisted state, schema bump, migration, Court module, dispatch ledger, policy ledger, court-process ledger, owner-lane ledger, cooldown ledger, memory-pressure ledger, public-reading ledger, public-follow-up ledger, docket ledger, household target field, Application rule layer, UI authority, Unity authority, or `PersonRegistry` expansion.

## Skill Alignment Through V132 - 2026-04-27

- V125-V132 continues the whole-skill pass: `zongzu-game-design` keeps the echo as delayed aftermath rather than a full court system; `zongzu-architecture-boundaries` keeps Office/PublicLife/SocialMemory ownership split; `zongzu-pressure-chain` keeps the path a rule-driven command / aftermath / social-memory / readback loop; `zongzu-save-and-schema` confirms no persisted state; `zongzu-simulation-validation` drives same-month/later-month proof; `zongzu-ui-shell` and `zongzu-unity-shell` keep presentation copy-only; `zongzu-content-authoring` protects `政策回应余味续接读回`; `zongzu-ancient-china` keeps the residue mediated by county documents, yamen posture, and public reading.
- The social-memory echo layer writes `office.policy_local_response...` memory from existing `JurisdictionAuthoritySnapshot` fields only. It must not become `order.public_life.response...`, home-household debt, or UI-computed court-policy success.
- Validation expectation: SocialMemory focused tests prove structured Office response input, no prose parsing, no Order mislabel; integration proves same-month neutrality and next-month durable residue; architecture tests guard no forbidden manager/ledger/schema drift; Unity presentation tests guard projected-field copy only.
- Save/schema result: no persisted state, schema bump, migration, Court module, dispatch ledger, policy ledger, court-process ledger, owner-lane ledger, cooldown ledger, social-memory ledger, household target field, Application rule layer, UI authority, Unity authority, or `PersonRegistry` expansion.
## Skill Alignment Through V244 - 2026-04-28

- The active whole-skill pass treats v213-v244 as the first implementation layer for the fidelity-ring/social-mobility model, not a full society engine and not per-person world simulation.
- `zongzu-game-design` keeps the player-facing rule as "near detail, far summary"; `zongzu-architecture-boundaries` assigns livelihood/activity/pools to `PopulationAndHouseholds` and identity/fidelity to `PersonRegistry`; `zongzu-save-and-schema` records no persisted shape change; `zongzu-simulation-validation` scopes proof to module, integration, architecture, presentation, and full solution tests; `zongzu-ui-shell` / `zongzu-unity-shell` keep great hall, desk sandbox, debug, and lineage surfaces copy-only.
- Performance/scheduler guidance for future v245+ work: name the hot path and cardinality before adding caches or more individual agents. This pass uses deterministic monthly/xun rebuilds over existing household/person/pool lists and runtime diagnostics rather than a saved ledger.
- Validation expectation: focused population tests prove livelihood/activity/pool rules; integration proves pressure-triggered focus-ring readback; architecture tests guard no prose parsing/no schema/no `PersonRegistry` expansion; Unity tests guard projected-field copy and JSON stability.

## Skill Alignment Through V252 - 2026-04-28

- The active whole-skill pass treats v245-v252 as a closeout audit over v213-v244, not a new runtime feature.
- `zongzu-architecture-boundaries` locks the owner split; `zongzu-save-and-schema` records that no persisted shape changed; `zongzu-simulation-validation` scopes proof to architecture/docs validation plus build/full tests; `zongzu-ui-shell` / `zongzu-unity-shell` keep mobility/fidelity surfaces copy-only; `zongzu-game-design` preserves "near detail, far summary" without claiming a full society engine.
- Future skill use after v252 should branch deliberately: use `zongzu-simulation-validation` for performance/cardinality/cadence work, `zongzu-save-and-schema` before any dormant-stub or durable movement-residue state, and `zongzu-architecture-boundaries` before letting force, office, relief, migration, or SocialMemory read the mobility substrate.
- Validation expectation: an architecture closeout test proves first-layer-only wording, explicit future debt, no schema/migration, no ledger, no prose parsing, no Application/UI/Unity authority, and no `PersonRegistry` expansion.

## Skill Alignment Through V260 - 2026-04-28

- The active whole-skill pass treats v253-v260 as Chain 9 first readback-thickening, not a full regime engine, faction AI, Court module, or dynasty-cycle simulation.
- `zongzu-game-design` keeps broad legitimacy pressure mediated through local office/public-life carriers; `zongzu-architecture-boundaries` keeps `WorldSettlements`, `OfficeAndCareer`, `PublicLifeAndRumor`, and `SocialMemoryAndRelations` in separate lanes; `zongzu-save-and-schema` records no persisted shape change; `zongzu-simulation-validation` scopes proof to same-month drain, off-scope non-inheritance, SocialMemory neutrality, architecture guards, Unity copy tests, and full solution tests.
- `zongzu-ui-shell` / `zongzu-unity-shell` require `天命摇动读回`, `去就风险读回`, `官身承压姿态`, and `公议向背读法` to arrive as projected fields only. `zongzu-content-authoring` and `zongzu-ancient-china` keep the wording historically mediated by mandate anxiety, yamen posture, dispatch/notice texture, and street reading rather than an emperor-control or household-repair surface.
- Save/schema result: no persisted state, schema bump, migration, full regime engine, legitimacy ledger, defection ledger, owner-lane ledger, cooldown ledger, same-month SocialMemory residue, Application rule layer, UI authority, Unity authority, manager/god-controller path, or `PersonRegistry` expansion.

## Skill Alignment Through V268 - 2026-04-28

- The active whole-skill pass treats v261-v268 as the closeout audit for the v253-v260 Chain 9 first readback branch, not a new runtime feature.
- `zongzu-architecture-boundaries` locks the owner split; `zongzu-pressure-chain` keeps the closure framed as first-layer readback rather than full regime-recognition completion; `zongzu-save-and-schema` records no persisted state; `zongzu-simulation-validation` scopes proof to architecture/docs validation plus build/full tests; `zongzu-ui-shell` / `zongzu-unity-shell` keep the shell copy-only; `zongzu-content-authoring` and `zongzu-ancient-china` preserve mandate, yamen, notice/dispatch, and street-reading carriers.
- Future skill use after v268 should open a new ExecPlan before adding regime-recognition state, public allegiance, ritual legitimacy, force backing, durable regime memory, or multi-pressure public-life arbitration.
- Validation expectation: an architecture closeout test proves first-readback-only wording, explicit future debt, no schema/migration, no ledger, no prose parsing, no Application/UI/Unity authority, no `PersonRegistry` expansion, and no production closeout authority.

## Skill Alignment Through V276 - 2026-04-28

- The active whole-skill pass treats v269-v276 as a scale-budget guard over the existing social mobility fidelity substrate, not a new runtime system.
- `zongzu-game-design` frames the design as near detail / far summary; `zongzu-architecture-boundaries` keeps ownership split between `PopulationAndHouseholds`, `PersonRegistry`, and later `SocialMemoryAndRelations`; `zongzu-save-and-schema` records no persisted shape change; `zongzu-simulation-validation` scopes proof to architecture/docs validation plus build/full tests; `zongzu-ui-shell` / `zongzu-unity-shell` keep shell surfaces copy-only.
- Future skill use after v276 should branch into real mobility/personnel-flow work only after naming hot path, expected cardinality, deterministic cap/order, owner module, schema impact, no-touch boundary, and validation lane.
- Validation expectation: an architecture guard proves four precision bands, no whole-world person simulation manager, no movement/social/focus/scheduler ledger, no prose parsing, no Application/UI/Unity authority, no `PersonRegistry` expansion, and no schema/migration drift.

## Skill Alignment Through V284 - 2026-04-28

- The active whole-skill pass treats v277-v284 as a projection/readback layer over existing mobility/fidelity rules, not a new social mobility engine.
- `zongzu-game-design` keeps the new fields player-readable without turning them into commands; `zongzu-architecture-boundaries` keeps Application as projection assembly; `zongzu-save-and-schema` confirms runtime read-model/ViewModel fields are no-save/no-schema; `zongzu-simulation-validation` scopes proof to focused integration, architecture, Unity JSON, build, diff, and full solution tests; `zongzu-ui-shell` / `zongzu-unity-shell` keep great hall, desk, lineage, and person inspector surfaces copy-only.
- Future skill use after v284 may deepen player influence only through a separate command-owner plan. It must not parse `InfluenceFootprintReadbackSummary`, `ScaleBudgetReadbackSummary`, movement prose, or `DomainEvent.Summary`.
- Validation expectation: focused tests prove the fields appear in read models and Unity ViewModels, while architecture guards prove no command, schema, ledger, manager, prose parsing, or `PersonRegistry` domain expansion.

## Skill Alignment Through V292 - 2026-04-28

- The active whole-skill pass treats v285-v292 as the closeout audit for v213-v284 social mobility / personnel-flow substrate, not a new runtime feature.
- `zongzu-game-design` locks the design as near detail / pressure-selected detail / active-region pools / distant summary; `zongzu-architecture-boundaries` keeps ownership split between `PopulationAndHouseholds`, `PersonRegistry`, and later `SocialMemoryAndRelations`; `zongzu-save-and-schema` records no persisted shape change; `zongzu-simulation-validation` scopes proof to architecture/docs validation plus build/full tests; `zongzu-ui-shell` / `zongzu-unity-shell` keep the shell copy-only and avoid implying direct personnel control.
- Future skill use after v292 should open a separate ExecPlan before adding personnel commands, durable movement residue, dormant stubs, class mobility formulas, migration economy, cross-region flow caches, or broader SocialMemory hooks.
- Validation expectation: an architecture closeout test proves first-layer-only wording, explicit future debt, no schema/migration, no ledger/cache/manager, no prose parsing, no Application/UI/Unity authority, no `PersonRegistry` expansion, and no production closeout authority.

## Skill Alignment Through V300 - 2026-04-28

- The active whole-skill pass treats v293-v300 as a personnel-command preflight audit, not a command implementation.
- `zongzu-game-design` keeps personnel movement as bounded leverage rather than player-as-god control; `zongzu-architecture-boundaries` requires owner-module resolution before any route enters `PlayerCommandCatalog`; `zongzu-save-and-schema` records no persisted shape change; `zongzu-simulation-validation` scopes proof to architecture/docs validation plus build/full tests; `zongzu-ui-shell` / `zongzu-unity-shell` keep future commands projected/copy-only.
- Future skill use after v300 may open a real command plan only after selecting the owner lane: `PopulationAndHouseholds` household migration response, `FamilyCore` kin/lineage mediation, `OfficeAndCareer` office/document service, `WarfareCampaign` campaign manpower posture, or a separately planned owner with schema and validation.
- Validation expectation: an architecture preflight test proves no direct move/transfer/summon/assign-person command, no command/movement/personnel/assignment ledger, no manager path, no prose parsing, no Application/UI/Unity authority, no `PersonRegistry` domain expansion, and no schema/migration drift.

## Skill Alignment Through V308 - 2026-04-28

- The active whole-skill pass treats v301-v308 as a projected readiness layer over existing `PopulationAndHouseholds` local response commands, not a direct personnel command implementation.
- `zongzu-game-design` keeps player leverage at the household pressure level; `zongzu-architecture-boundaries` keeps command resolution inside `PopulationAndHouseholds`; `zongzu-save-and-schema` records runtime read-model/ViewModel only; `zongzu-simulation-validation` scopes proof to focused integration, architecture, Unity presentation, build, diff, and full solution tests; `zongzu-ui-shell` / `zongzu-unity-shell` keep `PersonnelFlowReadinessSummary` copy-only.
- Future skill use after v308 may deepen personnel flow only by choosing a named owner lane and declaring target scope, hot path, expected cardinality, deterministic order/cap, no-touch boundary, schema impact, and validation lane.
- Validation expectation: focused tests prove `人员流动预备读回`, `近处细读`, and `远处汇总` appear on Population local-response affordances/receipts and are copied to Unity, while architecture guards prove no direct move/transfer/summon/assign-person route, no ledger/schema, no prose parsing, no Application/UI/Unity authority, and no `PersonRegistry` expansion.

## Skill Alignment Through V316 - 2026-04-28

- The active whole-skill pass treats v309-v316 as a surface echo over v301-v308 structured readiness fields, not a new command lane or migration engine.
- `zongzu-game-design` keeps the readback as bounded leverage; `zongzu-architecture-boundaries` keeps the echo in Application projection and shell display; `zongzu-save-and-schema` records runtime read-model only; `zongzu-simulation-validation` scopes proof to focused integration, architecture, Unity presentation, build, diff, and full solution tests; `zongzu-ui-shell` / `zongzu-unity-shell` keep Great Hall display copy-only.
- Future skill use after v316 may deepen personnel flow only after selecting an owner lane and proving target scope, hot path, expected cardinality, deterministic order/cap, schema impact, no-touch boundary, and validation lane.
- Validation expectation: focused tests prove `人员流动命令预备汇总` is assembled from structured command readiness fields and displayed in Great Hall mobility readback, while architecture guards prove no direct personnel command, no ledger/schema, no prose parsing, no Application/UI/Unity authority, and no `PersonRegistry` expansion.

## Skill Alignment Through V324 - 2026-04-28

- The active whole-skill pass treats v317-v324 as a closeout audit for v293-v316, not a runtime feature.
- `zongzu-game-design` closes the layer as bounded readiness; `zongzu-architecture-boundaries` prevents command/ledger/manager drift; `zongzu-save-and-schema` records no persisted change; `zongzu-simulation-validation` scopes proof to architecture, build, diff, and full solution tests; `zongzu-ui-shell` / `zongzu-unity-shell` keep existing public-life and Great Hall surfaces as projected readback only.
- Future skill use after v324 must open a new owner-lane plan before adding actual movement, migration, office-service, assignment, return, or campaign-manpower command rules.
- Validation expectation: the closeout guard proves v293-v316 is docs/tests closed as a first command-readiness layer, with no direct personnel command, no ledger/schema, no prose parsing, no Application/UI/Unity authority, and no `PersonRegistry` expansion.

## Skill Alignment Through V332 - 2026-04-28

- The active whole-skill pass treats v325-v332 as an owner-lane gate readback over existing command readiness fields, not a real personnel-flow command implementation.
- `zongzu-game-design` keeps the gate as bounded player guidance; `zongzu-architecture-boundaries` keeps current authority in `PopulationAndHouseholds` and future lanes as plans; `zongzu-save-and-schema` records runtime read-model only; `zongzu-simulation-validation` scopes proof to focused integration, architecture, Unity presentation, build, diff, and full solution tests; `zongzu-ui-shell` / `zongzu-unity-shell` keep Great Hall display copy-only.
- Future skill use after v332 may deepen exactly one owner lane only after declaring target scope, hot path, expected cardinality, deterministic order/cap, schema impact, no-touch boundary, and validation lane.
- Validation expectation: focused tests prove `人员流动归口门槛` is assembled from structured command fields and displayed in Great Hall mobility readback, while architecture guards prove no direct personnel command, no ledger/schema, no prose parsing, no Application/UI/Unity authority, and no `PersonRegistry` expansion.

## Skill Alignment Through V340 - 2026-04-28

- The active whole-skill pass treats v333-v340 as a Desk Sandbox echo of projected owner-lane gate text, not a new local movement rule.
- `zongzu-ui-shell` / `zongzu-unity-shell` keep the desk echo object-anchored and local to settlements with projected readiness commands; `zongzu-architecture-boundaries` keeps it in presentation enumeration; `zongzu-save-and-schema` records no persisted change; `zongzu-simulation-validation` scopes proof to focused architecture/presentation checks, build, diff, and full solution tests.
- Future skill use after v340 still must open a new owner-lane plan before adding movement, migration, office-service, assignment, return, or campaign-manpower command rules.
- Validation expectation: focused tests prove Desk Sandbox displays `人员流动归口门槛` only from structured local command readiness surfaces, while architecture guards prove no direct personnel command, no ledger/schema, no prose parsing, no Application/UI/Unity authority, and no `PersonRegistry` expansion.

## Skill Alignment Through V372 - 2026-04-28

- The active whole-skill pass treats v365-v372 as a future-lane preflight surface over existing command readiness, not an implementation of Family/Office/Warfare personnel-flow lanes.
- `zongzu-game-design` keeps the guidance as bounded leverage rather than direct person movement; `zongzu-architecture-boundaries` keeps the field in Application projection and Great Hall copy; `zongzu-save-and-schema` records runtime read-model only; `zongzu-simulation-validation` scopes proof to focused presentation/architecture checks, build, diff, and full tests; `zongzu-ui-shell` / `zongzu-unity-shell` keep Desk Sandbox from echoing the future preflight onto quiet/distant settlements.
- Future skill use after v372 should choose exactly one owner lane before adding any real command: Family kin/lineage mediation, Office document/service channels, Warfare campaign manpower, or a separately planned owner with schema and validation.
- Validation expectation: focused tests prove `人员流动未来归口预检` appears in Great Hall from `PersonnelFlowFutureOwnerLanePreflightSummary` and not on Desk settlement nodes, while architecture guards prove no direct personnel command, no ledger/schema, no prose parsing, no Application/UI/Unity authority, and no `PersonRegistry` expansion.

## Skill Alignment Through V380 - 2026-04-28

- The active whole-skill pass treats v373-v380 as a closeout audit for v357-v372, not a runtime feature.
- `zongzu-game-design` keeps future personnel-flow as owner-lane bounded leverage; `zongzu-architecture-boundaries` prevents the closeout from becoming command authority; `zongzu-save-and-schema` records no persisted change; `zongzu-simulation-validation` scopes proof to architecture/docs validation plus build/full tests; `zongzu-ui-shell` / `zongzu-unity-shell` keep the shell from presenting the preflight as a direct personnel board.
- Future skill use after v380 must open a new owner-lane plan before adding movement, migration, office-service, kin transfer, assignment, return, or campaign-manpower command rules.
- Validation expectation: the closeout guard proves v357-v372 is closed only as preflight visibility, with no direct personnel command, no ledger/schema, no prose parsing, no Application/UI/Unity authority, and no `PersonRegistry` expansion.

## Skill Alignment Through V388 - 2026-04-28

- The active whole-skill pass treats v381-v388 as commoner / social-position mobility preflight, not a runtime class engine.
- `zongzu-game-design` keeps class-position drift pressure-driven and scale-budgeted; `zongzu-architecture-boundaries` keeps future work in one declared owner lane; `zongzu-save-and-schema` records no persisted change; `zongzu-simulation-validation` scopes proof to docs/architecture guards; `zongzu-ui-shell` keeps person dossier labels from becoming promote/demote controls; `zongzu-ancient-china` keeps zhuhu/kehu, service, trade, clerk, artisan, and merchant pathways as historically plausible future carriers rather than fixed buttons.
- Future skill use after v388 must choose exactly one owner lane before adding commoner status drift, tenant/landholding conversion, office-service status, trade attachment, clerk route, artisan/merchant route, or durable social-position residue.
- Validation expectation: the preflight guard proves v381-v388 is docs/tests only, with no full class engine, no promote/demote command, no zhuhu/kehu conversion state, no ledger/schema, no prose parsing, no Application/UI/Unity authority, and no `PersonRegistry` expansion.

## Skill Alignment Through V396 - 2026-04-28

- The active whole-skill pass treats v389-v396 as a runtime readback layer over existing person dossier owner snapshots, not commoner status drift.
- `zongzu-game-design` keeps the field as legibility for bounded social position context; `zongzu-architecture-boundaries` keeps assembly in read models and copy in Unity; `zongzu-save-and-schema` records runtime read-model/ViewModel only; `zongzu-simulation-validation` scopes proof to focused integration, presentation, architecture, build, diff, and full tests; `zongzu-ui-shell` / `zongzu-unity-shell` keep the lineage inspector display-only; `zongzu-content-authoring` keeps the wording downstream of structured owner facts.
- Future skill use after v396 must still choose one owner lane before adding real commoner status drift, route history, tenant/landholding conversion, office-service status, trade attachment, clerk route, artisan/merchant route, or durable social-position residue.
- Validation expectation: focused tests prove `SocialPositionReadbackSummary` is projected from structured dossier sources and copied to Unity-facing ViewModels, while architecture guards prove no class engine, no promote/demote command, no zhuhu/kehu state, no ledger/schema, no prose parsing, no Application/UI/Unity authority, and no `PersonRegistry` expansion.

## Skill Alignment Through V404 - 2026-04-28

- The active whole-skill pass treats v397-v404 as structured provenance for the v389-v396 readback, not a new social-position authority layer.
- `zongzu-game-design` keeps the key list as legibility for bounded near-person context; `zongzu-architecture-boundaries` keeps it in read-model assembly; `zongzu-save-and-schema` records runtime read-model/ViewModel only; `zongzu-simulation-validation` scopes proof to focused integration, presentation, architecture, build, diff, and full tests; `zongzu-ui-shell` / `zongzu-unity-shell` keep the lineage inspector and Unity mirror copy-only.
- Future skill use after v404 must still choose one owner lane before adding real commoner status drift, route history, tenant/landholding conversion, office-service status, trade attachment, clerk route, artisan/merchant route, or durable social-position residue.
- Validation expectation: focused tests prove `SocialPositionSourceModuleKeys` is projected from structured dossier sources and copied to Unity-facing ViewModels, while architecture guards prove no class engine, no promote/demote command, no zhuhu/kehu state, no ledger/schema, no prose parsing, no Application/UI/Unity authority, and no `PersonRegistry` expansion.

## Skill Alignment Through V412 - 2026-04-28

- The active whole-skill pass treats v405-v412 as a closeout audit for v381-v404, not a runtime feature.
- `zongzu-game-design` keeps commoner status depth as future owner-lane work; `zongzu-architecture-boundaries` prevents the closeout from becoming authority; `zongzu-save-and-schema` records no persisted change; `zongzu-simulation-validation` scopes proof to architecture/docs validation plus build, diff, and full tests; `zongzu-ui-shell` / `zongzu-unity-shell` keep existing person dossier surfaces as projected readback only.
- Future skill use after v412 must open a new owner-lane plan before adding commoner status drift, tenant/landholding conversion, office-service status, trade attachment, clerk route, artisan/merchant route, durable social-position residue, or global per-person career simulation.
- Validation expectation: the closeout guard proves v381-v404 is closed only as preflight/readback/source-key visibility, with no class engine, no promote/demote command, no zhuhu/kehu state, no ledger/schema, no prose parsing, no Application/UI/Unity authority, and no `PersonRegistry` expansion.

## Skill Alignment Through V420 - 2026-04-28

- The active whole-skill pass treats v413-v420 as a runtime scale-budget readback over existing `FidelityRing` and structured source keys, not a precision mutation or class engine.
- `zongzu-game-design` keeps "near detail, far summary" visible without global per-person simulation; `zongzu-architecture-boundaries` keeps assembly in read models and display in copy-only presentation; `zongzu-save-and-schema` records runtime read-model/ViewModel only; `zongzu-simulation-validation` scopes proof to focused integration, presentation, architecture, build, diff, and full tests; `zongzu-ui-shell` / `zongzu-unity-shell` keep the lineage inspector display-only.
- Future skill use after v420 must still open a new owner-lane plan before changing precision bands, target cardinality, commoner status drift, tenant/landholding conversion, office-service status, trade attachment, clerk route, artisan/merchant route, or durable social-position residue.
- Validation expectation: focused tests prove `SocialPositionScaleBudgetReadbackSummary` is projected from existing `FidelityRing` and source keys and copied to Unity-facing ViewModels, while architecture guards prove no class engine, no ring mutation, no promote/demote command, no zhuhu/kehu state, no ledger/schema, no prose parsing, no Application/UI/Unity authority, and no `PersonRegistry` expansion.

## Skill Alignment Through V428 - 2026-04-28

- The active whole-skill pass treats v421-v428 as a far-summary guard over the v413-v420 scale-budget readback, not new runtime depth.
- `zongzu-game-design` protects the "distant society remains pooled" side; `zongzu-architecture-boundaries` keeps the check as tests/docs evidence over existing read-model assembly; `zongzu-save-and-schema` records no persisted change; `zongzu-simulation-validation` scopes proof to focused integration, architecture, build, diff, and full tests; `zongzu-ui-shell` / `zongzu-unity-shell` keep regional summary from becoming a person-browser or selection UI.
- Future skill use after v428 still needs a new owner-lane plan before changing regional precision, target selection, social-status drift, or durable residue.
- Validation expectation: focused tests prove a registry-only `FidelityRing.Regional` dossier reads `regional summary`, while architecture guards prove no class engine, no ring mutation, no ledger/schema, no prose parsing, no Application/UI/Unity authority, and no `PersonRegistry` expansion.

## Skill Alignment Through V436 - 2026-04-28

- The active whole-skill pass treats v429-v436 as closeout evidence over v381-v428, not a new runtime feature.
- `zongzu-game-design` keeps commoner status depth as future owner-lane work; `zongzu-architecture-boundaries` prevents the closeout from becoming authority; `zongzu-save-and-schema` records no persisted change; `zongzu-simulation-validation` scopes proof to architecture/docs validation plus build, diff, and full tests; `zongzu-ui-shell` / `zongzu-unity-shell` keep person dossier surfaces from becoming class ladders or global person browsers.
- Future skill use after v436 must open a new owner-lane plan before adding commoner status drift, tenant/landholding conversion, office-service status, trade attachment, clerk/artisan/merchant routes, regional person selection, or durable social-position residue.
- Validation expectation: the closeout guard proves v381-v428 is closed only as preflight/readback/source-key/scale-budget/regional-summary visibility, with no class engine, no promote/demote command, no zhuhu/kehu state, no ledger/schema, no prose parsing, no Application/UI/Unity authority, and no `PersonRegistry` expansion.

## Skill Alignment Through V444 - 2026-04-28

- The active whole-skill pass treats v437-v444 as future owner-lane preflight, not runtime commoner status depth.
- `zongzu-game-design` points the next rule pass toward household pressure rather than a class tree; `zongzu-architecture-boundaries` names `PopulationAndHouseholds` as the recommended future owner lane; `zongzu-save-and-schema` records no persisted change; `zongzu-simulation-validation` scopes proof to architecture/docs validation plus build, diff, and full tests; `zongzu-ui-shell` / `zongzu-unity-shell` keep future controls from appearing before projected owner affordances exist.
- Future skill use after v444 must write a real `PopulationAndHouseholds` or explicitly justified alternative owner-lane ExecPlan before adding commoner status drift, tenant/landholding conversion, office-service status, trade attachment, clerk/artisan/merchant routes, regional person selection, or durable social-position residue.
- Validation expectation: the preflight guard proves future status depth must not live in `PersonRegistry`, Application, UI, Unity, or a new standalone class module, and that v437-v444 adds no command, schema, ledger, prose parser, or production rule.

## Skill Alignment Through V452 - 2026-04-28

- The active whole-skill pass treats v445-v452 as a fidelity scale-budget preflight, not runtime detail promotion.
- `zongzu-game-design` keeps "near detail, far summary" as playable scale discipline; `zongzu-architecture-boundaries` prevents a global person scanner or manager/controller from appearing; `zongzu-save-and-schema` records no persisted change; `zongzu-simulation-validation` scopes proof to architecture/docs validation plus build, diff, and full tests; `zongzu-ui-shell` / `zongzu-unity-shell` keep far-region summaries from becoming selectable person browsers.
- Future skill use after v452 must declare target scope, hot path, touched counts, deterministic cap/order, cadence, schema impact, projection fields, and validation before changing fidelity rings, target cardinality, or commoner/personnel detail.
- Validation expectation: the preflight guard proves v445-v452 adds no fidelity-ring mutation, selector, global person scan, schema, ledger, prose parser, Application/UI/Unity authority, or `PersonRegistry` expansion.

## Skill Alignment Through V460 - 2026-04-28

- The active whole-skill pass treats v453-v460 as household mobility dynamics explanation over existing `PopulationAndHouseholds` signals, not a new mobility algorithm or class/status engine.
- `zongzu-game-design` keeps the readback as scale-budgeted explanation; `zongzu-architecture-boundaries` keeps ownership in `PopulationAndHouseholds` and identity in `PersonRegistry`; `zongzu-save-and-schema` records no persisted change; `zongzu-simulation-validation` scopes proof to focused integration/presentation/architecture plus build, diff, and full tests; `zongzu-ui-shell` / `zongzu-unity-shell` keep Desk Sandbox display copy-only.
- Future skill use after v460 must still declare owner state, cadence, target scope, hot path, touched counts, deterministic cap/order, schema impact, projection fields, and validation before adding commoner status drift, route history, durable residue, selector watermarks, or fidelity promotion.
- Validation expectation: the v453-v460 guard proves the explanation is assembled from structured household social-pressure signals and copied to Unity-facing surfaces, with no schema, ledger, parser, Application/UI/Unity authority, mobility engine, class engine, or `PersonRegistry` expansion.

## Household Mobility Closeout Through V468 - 2026-04-29

- The active whole-skill pass treats v461-v468 as closeout governance for v453-v460, not a runtime feature.
- The closeout keeps household mobility explanation bounded to existing `PopulationAndHouseholds` signals, deterministic dimension keys, read-model summary, and Desk Sandbox copy-only display.
- Future skill use after v468 must still declare owner state, cadence, target scope, hot path, touched counts, deterministic cap/order, schema impact, projection fields, and validation before adding household movement commands, route history, status drift, durable residue, selector watermarks, fidelity promotion, or distant per-person simulation.
- Validation expectation: the v461-v468 guard proves no production rule, schema, ledger, parser, Application/UI/Unity authority, mobility engine, class engine, route-history model, direct movement command, or `PersonRegistry` expansion.
- Household Mobility Runtime Threshold No-Touch Through V588 uses `zongzu-game-design`, `zongzu-architecture-boundaries`, `zongzu-save-and-schema`, `zongzu-simulation-validation`, `zongzu-pressure-chain`, `zongzu-content-authoring`, `zongzu-ancient-china`, `zongzu-ui-shell`, and `microsoft-code-reference` to keep the first runtime rule's active-pool threshold proof owner-laned, deterministic, schema-neutral, historically/plausibly framed as pressure evidence, and downstream-only for UI/presentation.
- Household Mobility Runtime Zero-Cap No-Touch Through V596 uses `zongzu-game-design`, `zongzu-architecture-boundaries`, `zongzu-save-and-schema`, `zongzu-simulation-validation`, `zongzu-pressure-chain`, `zongzu-content-authoring`, `zongzu-ancient-china`, `zongzu-ui-shell`, and `microsoft-code-reference` to keep the first runtime rule's zero-cap proof owner-laned, deterministic, schema-neutral, historically/plausibly framed as pressure evidence, and downstream-only for UI/presentation.
- Household Mobility Runtime Zero-Risk-Delta No-Touch Through V604 uses `zongzu-game-design`, `zongzu-architecture-boundaries`, `zongzu-save-and-schema`, `zongzu-simulation-validation`, `zongzu-pressure-chain`, `zongzu-content-authoring`, `zongzu-ancient-china`, `zongzu-ui-shell`, and `microsoft-code-reference` to keep the first runtime rule's zero-risk-delta proof owner-laned, deterministic, schema-neutral, historically/plausibly framed as pressure evidence, and downstream-only for UI/presentation.
- Household Mobility Runtime Candidate Filter No-Touch Through V612 uses `zongzu-game-design`, `zongzu-architecture-boundaries`, `zongzu-save-and-schema`, `zongzu-simulation-validation`, `zongzu-pressure-chain`, `zongzu-content-authoring`, `zongzu-ancient-china`, `zongzu-ui-shell`, and `microsoft-code-reference` to keep the first runtime rule's candidate-filter proof owner-laned, deterministic, schema-neutral, historically/plausibly framed as pressure evidence, and downstream-only for UI/presentation.
- Household Mobility Runtime Tie-Break No-Touch Through V620 uses `zongzu-game-design`, `zongzu-architecture-boundaries`, `zongzu-save-and-schema`, `zongzu-simulation-validation`, `zongzu-pressure-chain`, `zongzu-content-authoring`, `zongzu-ancient-china`, `zongzu-ui-shell`, and `microsoft-code-reference` to keep the first runtime rule's deterministic ordering proof owner-laned, schema-neutral, historically/plausibly framed as pressure evidence, and downstream-only for UI/presentation.
- Household Mobility Runtime Pool Tie-Break No-Touch Through V628 uses `zongzu-game-design`, `zongzu-architecture-boundaries`, `zongzu-save-and-schema`, `zongzu-simulation-validation`, `zongzu-pressure-chain`, `zongzu-content-authoring`, `zongzu-ancient-china`, `zongzu-ui-shell`, and `microsoft-code-reference` to keep the first runtime rule's active-pool deterministic ordering proof owner-laned, schema-neutral, historically/plausibly framed as pressure evidence, and downstream-only for UI/presentation.
- Household Mobility Runtime Score-Ordering No-Touch Through V636 uses `zongzu-game-design`, `zongzu-architecture-boundaries`, `zongzu-save-and-schema`, `zongzu-simulation-validation`, `zongzu-pressure-chain`, `zongzu-content-authoring`, `zongzu-ancient-china`, `zongzu-ui-shell`, and `microsoft-code-reference` to keep the first runtime rule's candidate score-ordering proof owner-laned, schema-neutral, historically/plausibly framed as pressure evidence, and downstream-only for UI/presentation.
- Household Mobility Runtime Pool-Priority No-Touch Through V644 uses `zongzu-game-design`, `zongzu-architecture-boundaries`, `zongzu-save-and-schema`, `zongzu-simulation-validation`, `zongzu-pressure-chain`, `zongzu-content-authoring`, `zongzu-ancient-china`, `zongzu-ui-shell`, and `microsoft-code-reference` to keep the first runtime rule's active-pool priority before cross-pool household score proof owner-laned, schema-neutral, historically/plausibly framed as pressure evidence, and downstream-only for UI/presentation.
- Household Mobility Runtime Per-Pool Cap No-Touch Through V652 uses `zongzu-game-design`, `zongzu-architecture-boundaries`, `zongzu-save-and-schema`, `zongzu-simulation-validation`, `zongzu-pressure-chain`, `zongzu-content-authoring`, `zongzu-ancient-china`, `zongzu-ui-shell`, and `microsoft-code-reference` to keep the first runtime rule's per-selected-pool household cap proof owner-laned, schema-neutral, historically/plausibly framed as pressure evidence, and downstream-only for UI/presentation.
- Household Mobility Runtime Threshold Event No-Touch Through V660 uses `zongzu-game-design`, `zongzu-architecture-boundaries`, `zongzu-save-and-schema`, `zongzu-simulation-validation`, `zongzu-pressure-chain`, `zongzu-content-authoring`, `zongzu-ancient-china`, `zongzu-ui-shell`, and `microsoft-code-reference` to keep the first runtime rule's selected-household threshold event proof owner-laned, schema-neutral, structured-event-only, historically/plausibly framed as pressure evidence, and downstream-only for UI/presentation.
- Household Mobility Runtime Event Metadata No-Prose Through V668 uses `zongzu-game-design`, `zongzu-architecture-boundaries`, `zongzu-save-and-schema`, `zongzu-simulation-validation`, `zongzu-pressure-chain`, `zongzu-content-authoring`, `zongzu-ancient-china`, `zongzu-ui-shell`, and `microsoft-code-reference` to keep the first runtime rule's selected-household event metadata proof owner-laned, schema-neutral, no-prose-parser, historically/plausibly framed as pressure evidence, and downstream-only for UI/presentation.
- Household Mobility Runtime Event Metadata Replay Through V676 uses `zongzu-game-design`, `zongzu-architecture-boundaries`, `zongzu-save-and-schema`, `zongzu-simulation-validation`, `zongzu-pressure-chain`, `zongzu-content-authoring`, `zongzu-ancient-china`, `zongzu-ui-shell`, and `microsoft-code-reference` to keep the first runtime rule's selected-household event metadata replay proof owner-laned, schema-neutral, no-replay-state, historically/plausibly framed as pressure evidence, and downstream-only for UI/presentation.
- Household Mobility Runtime Threshold Extraction Through V684 uses `zongzu-game-design`, `zongzu-architecture-boundaries`, `zongzu-save-and-schema`, `zongzu-simulation-validation`, `zongzu-pressure-chain`, `zongzu-content-authoring`, `zongzu-ancient-china`, `zongzu-ui-shell`, and `microsoft-code-reference` to keep the first runtime rule's selected-household event threshold extraction owner-consumed, default-equivalent, schema-neutral, loader-free, historically/plausibly framed as pressure evidence, and downstream-only for UI/presentation.
- Household Mobility Runtime Candidate Floor Extraction Through V692 uses `zongzu-game-design`, `zongzu-architecture-boundaries`, `zongzu-save-and-schema`, `zongzu-simulation-validation`, `zongzu-pressure-chain`, `zongzu-content-authoring`, `zongzu-ancient-china`, `zongzu-ui-shell`, and `microsoft-code-reference` to keep the first runtime rule's low-risk candidate floor extraction owner-consumed, default-equivalent, schema-neutral, loader-free, historically/plausibly framed as pressure evidence, and downstream-only for UI/presentation.
- Household Mobility Runtime Score Weight Extraction Through V700 uses `zongzu-game-design`, `zongzu-architecture-boundaries`, `zongzu-save-and-schema`, `zongzu-simulation-validation`, `zongzu-pressure-chain`, `zongzu-content-authoring`, `zongzu-ancient-china`, `zongzu-ui-shell`, and `microsoft-code-reference` to keep the first runtime rule's migration-risk score weight extraction owner-consumed, default-equivalent, schema-neutral, loader-free, historically/plausibly framed as pressure evidence, and downstream-only for UI/presentation.
- Household Mobility Runtime Labor Floor Extraction Through V708 uses `zongzu-game-design`, `zongzu-architecture-boundaries`, `zongzu-save-and-schema`, `zongzu-simulation-validation`, `zongzu-pressure-chain`, `zongzu-content-authoring`, `zongzu-ancient-china`, `zongzu-ui-shell`, and `microsoft-code-reference` to keep the first runtime rule's labor-capacity pressure floor extraction owner-consumed, default-equivalent, schema-neutral, loader-free, historically/plausibly framed as pressure evidence, and downstream-only for UI/presentation.
- Household Mobility Runtime Grain Floor Extraction Through V716 uses `zongzu-game-design`, `zongzu-architecture-boundaries`, `zongzu-save-and-schema`, `zongzu-simulation-validation`, `zongzu-pressure-chain`, `zongzu-content-authoring`, `zongzu-ancient-china`, `zongzu-ui-shell`, and `microsoft-code-reference` to keep the first runtime rule's grain-store pressure floor extraction owner-consumed, default-equivalent, schema-neutral, loader-free, historically/plausibly framed as pressure evidence, and downstream-only for UI/presentation.
- Household Mobility Runtime Land Floor Extraction Through V724 uses `zongzu-game-design`, `zongzu-architecture-boundaries`, `zongzu-save-and-schema`, `zongzu-simulation-validation`, `zongzu-pressure-chain`, `zongzu-content-authoring`, `zongzu-ancient-china`, `zongzu-ui-shell`, and `microsoft-code-reference` to keep the first runtime rule's land-holding pressure floor extraction owner-consumed, default-equivalent, schema-neutral, loader-free, historically/plausibly framed as pressure evidence, and downstream-only for UI/presentation.
- Household Mobility Runtime Grain Divisor Extraction Through V732 uses `zongzu-game-design`, `zongzu-architecture-boundaries`, `zongzu-save-and-schema`, `zongzu-simulation-validation`, `zongzu-pressure-chain`, `zongzu-content-authoring`, `zongzu-ancient-china`, `zongzu-ui-shell`, and `microsoft-code-reference` to keep the first runtime rule's grain-store pressure divisor extraction owner-consumed, default-equivalent, schema-neutral, loader-free, divide-by-zero guarded, historically/plausibly framed as pressure evidence, and downstream-only for UI/presentation.
- Household Mobility Runtime Land Divisor Extraction Through V740 uses `zongzu-game-design`, `zongzu-architecture-boundaries`, `zongzu-save-and-schema`, `zongzu-simulation-validation`, `zongzu-pressure-chain`, `zongzu-content-authoring`, `zongzu-ancient-china`, `zongzu-ui-shell`, and `microsoft-code-reference` to keep the first runtime rule's land-holding pressure divisor extraction owner-consumed, default-equivalent, schema-neutral, loader-free, divide-by-zero guarded, historically/plausibly framed as pressure evidence, and downstream-only for UI/presentation.
- Household Mobility Runtime Candidate Ceiling Extraction Through V748 uses `zongzu-game-design`, `zongzu-architecture-boundaries`, `zongzu-save-and-schema`, `zongzu-simulation-validation`, `zongzu-pressure-chain`, `zongzu-content-authoring`, `zongzu-ancient-china`, `zongzu-ui-shell`, and `microsoft-code-reference` to keep the first runtime rule's high-risk candidate ceiling extraction owner-consumed, default-equivalent, schema-neutral, loader-free, historically/plausibly framed as pressure/no-touch evidence, and downstream-only for UI/presentation.
- Household Mobility Runtime Distress Trigger Extraction Through V756 uses `zongzu-game-design`, `zongzu-architecture-boundaries`, `zongzu-save-and-schema`, `zongzu-simulation-validation`, `zongzu-pressure-chain`, `zongzu-content-authoring`, `zongzu-ancient-china`, `zongzu-ui-shell`, and `microsoft-code-reference` to keep the first runtime rule's distress trigger extraction owner-consumed, default-equivalent, schema-neutral, loader-free, historically/plausibly framed as household pressure evidence, and downstream-only for UI/presentation.
- Household Mobility Runtime Debt Trigger Extraction Through V764 uses `zongzu-game-design`, `zongzu-architecture-boundaries`, `zongzu-save-and-schema`, `zongzu-simulation-validation`, `zongzu-pressure-chain`, `zongzu-content-authoring`, `zongzu-ancient-china`, `zongzu-ui-shell`, and `microsoft-code-reference` to keep the first runtime rule's debt-pressure trigger extraction owner-consumed, default-equivalent, schema-neutral, loader-free, historically/plausibly framed as household pressure evidence, and downstream-only for UI/presentation.
- Household Mobility Runtime Labor Trigger Extraction Through V772 uses `zongzu-game-design`, `zongzu-architecture-boundaries`, `zongzu-save-and-schema`, `zongzu-simulation-validation`, `zongzu-pressure-chain`, `zongzu-content-authoring`, `zongzu-ancient-china`, `zongzu-ui-shell`, and `microsoft-code-reference` to keep the first runtime rule's labor-capacity trigger extraction owner-consumed, default-equivalent, schema-neutral, loader-free, historically/plausibly framed as household pressure evidence, and downstream-only for UI/presentation.
- Household Mobility Runtime Grain Trigger Extraction Through V780 uses `zongzu-game-design`, `zongzu-architecture-boundaries`, `zongzu-save-and-schema`, `zongzu-simulation-validation`, `zongzu-pressure-chain`, `zongzu-content-authoring`, `zongzu-ancient-china`, `zongzu-ui-shell`, and `microsoft-code-reference` to keep the first runtime rule's grain-store trigger extraction owner-consumed, default-equivalent, schema-neutral, loader-free, historically/plausibly framed as household pressure evidence, and downstream-only for UI/presentation.
- Household Mobility Runtime Land Trigger Extraction Through V788 uses `zongzu-game-design`, `zongzu-architecture-boundaries`, `zongzu-save-and-schema`, `zongzu-simulation-validation`, `zongzu-pressure-chain`, `zongzu-content-authoring`, `zongzu-ancient-china`, `zongzu-ui-shell`, and `microsoft-code-reference` to keep the first runtime rule's land-holding trigger extraction owner-consumed, default-equivalent, schema-neutral, loader-free, historically/plausibly framed as household pressure evidence, and downstream-only for UI/presentation.
- Household Mobility Runtime Livelihood Trigger Extraction Through V796 uses `zongzu-game-design`, `zongzu-architecture-boundaries`, `zongzu-save-and-schema`, `zongzu-simulation-validation`, `zongzu-pressure-chain`, `zongzu-content-authoring`, `zongzu-ancient-china`, `zongzu-ui-shell`, and `microsoft-code-reference` to keep the first runtime rule's livelihood trigger extraction owner-consumed, default-equivalent, schema-neutral, loader-free, historically/plausibly framed as household pressure evidence, and downstream-only for UI/presentation.
- Household Mobility Runtime Livelihood Score Weight Extraction Through V804 uses `zongzu-game-design`, `zongzu-architecture-boundaries`, `zongzu-save-and-schema`, `zongzu-simulation-validation`, `zongzu-pressure-chain`, `zongzu-content-authoring`, `zongzu-ancient-china`, `zongzu-ui-shell`, and `microsoft-code-reference` to keep the first runtime rule's livelihood score weights owner-consumed, default-equivalent, schema-neutral, loader-free, historically/plausibly framed as household pressure ordering, and downstream-only for UI/presentation.
- Household Mobility Runtime Pressure Score Weight Extraction Through V812 uses `zongzu-game-design`, `zongzu-architecture-boundaries`, `zongzu-save-and-schema`, `zongzu-simulation-validation`, `zongzu-pressure-chain`, `zongzu-content-authoring`, `zongzu-ancient-china`, `zongzu-ui-shell`, and `microsoft-code-reference` to keep the first runtime rule's distress/debt pressure score weights owner-consumed, default-equivalent, schema-neutral, loader-free, historically/plausibly framed as household pressure ordering, and downstream-only for UI/presentation.
- Household Mobility Runtime Migration Status Threshold Extraction Through V820 uses `zongzu-game-design`, `zongzu-architecture-boundaries`, `zongzu-save-and-schema`, `zongzu-simulation-validation`, `zongzu-pressure-chain`, `zongzu-content-authoring`, `zongzu-ancient-china`, `zongzu-ui-shell`, and `microsoft-code-reference` to keep the first runtime rule's migration status threshold owner-consumed, default-equivalent, schema-neutral, loader-free, historically/plausibly framed as thresholded household pressure state, and downstream-only for UI/presentation.
- Household Mobility Runtime Migration Risk Clamp Extraction Through V828 uses `zongzu-game-design`, `zongzu-architecture-boundaries`, `zongzu-save-and-schema`, `zongzu-simulation-validation`, `zongzu-pressure-chain`, `zongzu-content-authoring`, `zongzu-ancient-china`, `zongzu-ui-shell`, and `microsoft-code-reference` to keep the first runtime rule's migration-risk clamp bounds owner-consumed, default-equivalent, schema-neutral, loader-free, historically/plausibly framed as bounded household pressure state, and downstream-only for UI/presentation.
- Household Mobility Runtime Tie-Break Priority Extraction Through V836 uses `zongzu-game-design`, `zongzu-architecture-boundaries`, `zongzu-save-and-schema`, `zongzu-simulation-validation`, `zongzu-pressure-chain`, `zongzu-content-authoring`, `zongzu-ancient-china`, `zongzu-ui-shell`, and `microsoft-code-reference` to keep the first runtime rule's active-pool and household tie-break priorities owner-consumed, default-equivalent, schema-neutral, loader-free, historically/plausibly framed as deterministic household pressure ordering, and downstream-only for UI/presentation.
- Household Mobility Runtime Unmatched Livelihood Score Extraction Through V844 uses `zongzu-game-design`, `zongzu-architecture-boundaries`, `zongzu-save-and-schema`, `zongzu-simulation-validation`, `zongzu-pressure-chain`, `zongzu-content-authoring`, `zongzu-ancient-china`, `zongzu-ui-shell`, and `microsoft-code-reference` to keep the first runtime rule's unmatched-livelihood score fallback owner-consumed, default-equivalent, schema-neutral, loader-free, historically/plausibly framed as bounded household pressure ordering, and downstream-only for UI/presentation.
- Household Mobility Runtime Pressure Contribution Floor Extraction Through V852 uses `zongzu-game-design`, `zongzu-architecture-boundaries`, `zongzu-save-and-schema`, `zongzu-simulation-validation`, `zongzu-pressure-chain`, `zongzu-content-authoring`, `zongzu-ancient-china`, `zongzu-ui-shell`, and `microsoft-code-reference` to keep the first runtime rule's pressure contribution floor owner-consumed, default-equivalent, schema-neutral, loader-free, historically/plausibly framed as non-negative household pressure ordering, and downstream-only for UI/presentation.
- Household Mobility Runtime Extraction Closeout Through V860 uses `zongzu-game-design`, `zongzu-architecture-boundaries`, `zongzu-save-and-schema`, `zongzu-simulation-validation`, `zongzu-pressure-chain`, `zongzu-content-authoring`, `zongzu-ancient-china`, `zongzu-ui-shell`, and `microsoft-code-reference` to close the first runtime rule extraction track, classify remaining inline guards as non-extraction control flow, keep the pass schema-neutral, loader-free, historically/plausibly framed as bounded household pressure selection, and downstream-only for UI/presentation.
- PopulationAndHouseholds Runtime Rule File Split Through V868 uses `zongzu-game-design`, `zongzu-architecture-boundaries`, `zongzu-save-and-schema`, `zongzu-simulation-validation`, `zongzu-pressure-chain`, `zongzu-content-authoring`, `zongzu-ancient-china`, `zongzu-ui-shell`, and `microsoft-code-reference` to split the first household mobility runtime rule into a partial owner file while preserving behavior, schema neutrality, loader-free rules-data boundaries, and downstream-only UI/presentation.
- PopulationAndHouseholds Membership Focus File Split Through V876 uses `zongzu-game-design`, `zongzu-architecture-boundaries`, `zongzu-save-and-schema`, `zongzu-simulation-validation`, `zongzu-pressure-chain`, `zongzu-content-authoring`, `zongzu-ancient-china`, `zongzu-ui-shell`, and `microsoft-code-reference` to split membership synchronization and hot-household focus promotion into a partial owner file while preserving behavior, schema neutrality, loader-free rules-data boundaries, `PersonRegistry` command seams, and downstream-only UI/presentation.
- PopulationAndHouseholds Pool Rebuild File Split Through V884 uses `zongzu-game-design`, `zongzu-architecture-boundaries`, `zongzu-save-and-schema`, `zongzu-simulation-validation`, `zongzu-pressure-chain`, `zongzu-content-authoring`, `zongzu-ancient-china`, `zongzu-ui-shell`, and `microsoft-code-reference` to split settlement summary and labor/marriage/migration pool rebuilds into a partial owner file while preserving behavior, schema neutrality, loader-free boundaries, `PersonRegistry` query seams, and downstream-only UI/presentation.
- PopulationAndHouseholds Query Surface File Split Through V892 uses `zongzu-game-design`, `zongzu-architecture-boundaries`, `zongzu-save-and-schema`, `zongzu-simulation-validation`, `zongzu-pressure-chain`, `zongzu-content-authoring`, `zongzu-ancient-china`, `zongzu-ui-shell`, and `microsoft-code-reference` to split the private query implementation and clone helpers into a partial owner file while preserving behavior, schema neutrality, loader-free boundaries, deterministic snapshot ordering, and downstream-only UI/presentation.
- PopulationAndHouseholds Pressure Profile File Split Through V900 uses `zongzu-game-design`, `zongzu-architecture-boundaries`, `zongzu-save-and-schema`, `zongzu-simulation-validation`, `zongzu-pressure-chain`, `zongzu-content-authoring`, `zongzu-ancient-china`, `zongzu-ui-shell`, and `microsoft-code-reference` to split grain/tax/official-supply pressure profile helpers into a partial owner file while preserving behavior, schema neutrality, loader-free boundaries, metadata fallback values, and downstream-only UI/presentation.
- PopulationAndHouseholds Event Dispatch File Split Through V908 uses `zongzu-game-design`, `zongzu-architecture-boundaries`, `zongzu-save-and-schema`, `zongzu-simulation-validation`, `zongzu-pressure-chain`, `zongzu-content-authoring`, `zongzu-ancient-china`, `zongzu-ui-shell`, and `microsoft-code-reference` to split trade/world/family/office event dispatch helpers into a partial owner file while preserving behavior, schema neutrality, event metadata, deterministic ordering, no prose parsing, and downstream-only UI/presentation.
- PopulationAndHouseholds Livelihood Drift File Split Through V916 uses `zongzu-game-design`, `zongzu-architecture-boundaries`, `zongzu-save-and-schema`, `zongzu-simulation-validation`, `zongzu-pressure-chain`, `zongzu-content-authoring`, `zongzu-ancient-china`, `zongzu-ui-shell`, and `microsoft-code-reference` to split monthly livelihood drift helpers into a partial owner file while preserving behavior, schema neutrality, livelihood thresholds, baseline mapping, no class/status engine, no prose parsing, and downstream-only UI/presentation.
- PopulationAndHouseholds Monthly Pulse File Split Through V924 uses `zongzu-game-design`, `zongzu-architecture-boundaries`, `zongzu-save-and-schema`, `zongzu-simulation-validation`, `zongzu-pressure-chain`, `zongzu-content-authoring`, `zongzu-ancient-china`, `zongzu-ui-shell`, and `microsoft-code-reference` to split xun/month pulse and migration-status helpers into a partial owner file while preserving behavior, schema neutrality, delta thresholds, migration status fallback, no class/status engine, no prose parsing, and downstream-only UI/presentation.
- PopulationAndHouseholds Warfare Aftermath File Split Through V932 uses `zongzu-game-design`, `zongzu-architecture-boundaries`, `zongzu-save-and-schema`, `zongzu-simulation-validation`, `zongzu-pressure-chain`, `zongzu-content-authoring`, `zongzu-ancient-china`, `zongzu-ui-shell`, and `microsoft-code-reference` to split warfare aftermath handling into a partial owner file while preserving behavior, schema neutrality, campaign delta formulas, settlement-scoped household ordering, no class/status engine, no prose parsing, and downstream-only UI/presentation.
- PopulationAndHouseholds Health Lifecycle File Split Through V940 uses `zongzu-game-design`, `zongzu-architecture-boundaries`, `zongzu-save-and-schema`, `zongzu-simulation-validation`, `zongzu-pressure-chain`, `zongzu-content-authoring`, `zongzu-ancient-china`, `zongzu-ui-shell`, and `microsoft-code-reference` to split monthly health lifecycle handling into a partial owner file while preserving behavior, schema neutrality, health thresholds, illness-month caps, person-id membership ordering, no class/status engine, no prose parsing, no `PersonRegistry` expansion, and downstream-only UI/presentation.
- PopulationAndHouseholds Monthly Pressure Rules-Data Extraction Through V948 uses `zongzu-game-design`, `zongzu-architecture-boundaries`, `zongzu-save-and-schema`, `zongzu-simulation-validation`, `zongzu-pressure-chain`, `zongzu-content-authoring`, `zongzu-ancient-china`, `zongzu-ui-shell`, and `microsoft-code-reference` to extract monthly prosperity/security/clan-relief/drift thresholds from `RunMonth` C# literals into owner-consumed rules-data while preserving default behavior, schema neutrality, no loader/plugin system, no class/status engine, no prose parsing, no `PersonRegistry` expansion, and downstream-only UI/presentation.
- PopulationAndHouseholds Grain Price Signal Rules-Data Extraction Through V956 uses `zongzu-game-design`, `zongzu-architecture-boundaries`, `zongzu-save-and-schema`, `zongzu-simulation-validation`, `zongzu-pressure-chain`, `zongzu-content-authoring`, `zongzu-ancient-china`, `zongzu-ui-shell`, and `microsoft-code-reference` to extract grain-price shock metadata fallback and clamp values from C# literals into owner-consumed rules-data while preserving default behavior, schema neutrality, no loader/plugin system, no class/status engine, no prose parsing, no `PersonRegistry` expansion, and downstream-only UI/presentation.
- PopulationAndHouseholds Grain Price Pressure Clamp Extraction Through V964 uses `zongzu-game-design`, `zongzu-architecture-boundaries`, `zongzu-save-and-schema`, `zongzu-simulation-validation`, `zongzu-pressure-chain`, `zongzu-content-authoring`, `zongzu-ancient-china`, `zongzu-ui-shell`, and `microsoft-code-reference` to extract grain-price price-pressure clamp bounds from C# literals into owner-consumed rules-data while preserving default behavior, schema neutrality, no loader/plugin system, no class/status engine, no prose parsing, no `PersonRegistry` expansion, and downstream-only UI/presentation.
- PopulationAndHouseholds Grain Price Level Band Extraction Through V972 uses `zongzu-game-design`, `zongzu-architecture-boundaries`, `zongzu-save-and-schema`, `zongzu-simulation-validation`, `zongzu-pressure-chain`, `zongzu-content-authoring`, `zongzu-ancient-china`, `zongzu-ui-shell`, and `microsoft-code-reference` to extract grain-price level threshold/score bands from C# literals into owner-consumed rules-data while preserving default behavior, schema neutrality, no loader/plugin system, no class/status engine, no prose parsing, no `PersonRegistry` expansion, and downstream-only UI/presentation.
- PopulationAndHouseholds Grain Price Jump Band Extraction Through V980 uses `zongzu-game-design`, `zongzu-architecture-boundaries`, `zongzu-save-and-schema`, `zongzu-simulation-validation`, `zongzu-pressure-chain`, `zongzu-content-authoring`, `zongzu-ancient-china`, `zongzu-ui-shell`, and `microsoft-code-reference` to extract grain-price jump threshold/score bands from C# literals into owner-consumed rules-data while preserving default behavior, schema neutrality, no loader/plugin system, no class/status engine, no prose parsing, no `PersonRegistry` expansion, and downstream-only UI/presentation.
- PopulationAndHouseholds Grain Price Market Tightness Band Extraction Through V988 uses `zongzu-game-design`, `zongzu-architecture-boundaries`, `zongzu-save-and-schema`, `zongzu-simulation-validation`, `zongzu-pressure-chain`, `zongzu-content-authoring`, `zongzu-ancient-china`, `zongzu-ui-shell`, and `microsoft-code-reference` to extract grain-price market tightness threshold/score bands from C# literals into owner-consumed rules-data while preserving default behavior, schema neutrality, no loader/plugin system, no class/status engine, no prose parsing, no `PersonRegistry` expansion, and downstream-only UI/presentation.
- PopulationAndHouseholds Subsistence Market Dependency Extraction Through V996 uses `zongzu-game-design`, `zongzu-architecture-boundaries`, `zongzu-save-and-schema`, `zongzu-simulation-validation`, `zongzu-pressure-chain`, `zongzu-content-authoring`, `zongzu-ancient-china`, `zongzu-ui-shell`, and `microsoft-code-reference` to extract subsistence market dependency livelihood score weights from C# literals into owner-consumed rules-data while preserving default behavior, schema neutrality, no loader/plugin system, no class/status engine, no prose parsing, no `PersonRegistry` expansion, and downstream-only UI/presentation.
- PopulationAndHouseholds Subsistence Labor Capacity Extraction Through V1004 uses `zongzu-game-design`, `zongzu-architecture-boundaries`, `zongzu-save-and-schema`, `zongzu-simulation-validation`, `zongzu-pressure-chain`, `zongzu-content-authoring`, `zongzu-ancient-china`, `zongzu-ui-shell`, and `microsoft-code-reference` to extract subsistence labor-capacity threshold score bands from C# literals into owner-consumed rules-data while preserving default behavior, schema neutrality, no loader/plugin system, no class/status engine, no prose parsing, no `PersonRegistry` expansion, and downstream-only UI/presentation.
- PopulationAndHouseholds Subsistence Dependent Count Extraction Through V1012 uses `zongzu-game-design`, `zongzu-architecture-boundaries`, `zongzu-save-and-schema`, `zongzu-simulation-validation`, `zongzu-pressure-chain`, `zongzu-content-authoring`, `zongzu-ancient-china`, `zongzu-ui-shell`, and `microsoft-code-reference` to extract subsistence dependent-count threshold score bands from C# literals into owner-consumed rules-data while preserving default behavior, schema neutrality, no loader/plugin system, no class/status engine, no prose parsing, no `PersonRegistry` expansion, and downstream-only UI/presentation.
- PopulationAndHouseholds Subsistence Labor Clamp Extraction Through V1020 uses `zongzu-game-design`, `zongzu-architecture-boundaries`, `zongzu-save-and-schema`, `zongzu-simulation-validation`, `zongzu-pressure-chain`, `zongzu-content-authoring`, `zongzu-ancient-china`, `zongzu-ui-shell`, and `microsoft-code-reference` to extract the final subsistence labor pressure clamp from C# literals into owner-consumed rules-data while preserving default behavior, schema neutrality, no loader/plugin system, no class/status engine, no prose parsing, no `PersonRegistry` expansion, and downstream-only UI/presentation.
