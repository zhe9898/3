# SCHEMA_NAMESPACE_RULES

This document defines how save data and state namespaces stay extensible.

## Root save layout
The save root should contain:
- root schema version
- feature manifest
- kernel/common state
- module state dictionary keyed by module name
- replay metadata
- debug metadata if applicable

Example conceptual shape:
```json
{
  "rootSchemaVersion": 1,
  "featureManifest": {
    "FamilyCore": "full",
    "EducationAndExams": "lite",
    "TradeAndIndustry": "lite",
    "OrderAndBanditry": "off"
  },
  "kernelState": { "...": "..." },
  "modules": {
    "WorldSettlements": { "moduleSchemaVersion": 2, "state": { } },
    "FamilyCore": { "moduleSchemaVersion": 3, "state": { } }
  }
}
```

## Namespace rules
- each module owns one top-level save namespace
- module namespaces may contain sub-namespaces
- no module writes fields into another module namespace
- common/core state stays small and stable

## Core vs module state
### Core state should contain only:
- typed ID allocators
- global date
- global RNG/replay metadata
- shared entity identity skeletons if needed
- enabled feature manifest

### Module state should contain:
- all feature-specific authoritative state
- module-local indexes
- module-local projections if persisted deliberately
- module-local version number

## Entity data pattern
Do not create a single fat `Person` or `Clan` save blob with every feature field inside.
Prefer:
- small core identity record
- module-owned per-entity state in module namespace

Example:
- core person identity in kernel/common
- exam progress in `EducationAndExams`
- trade role in `TradeAndIndustry`
- office rank in `OfficeAndCareer`
- outlaw risk in `OrderAndBanditry`
- force role in `ConflictAndForce`

## Migration rules
- root schema migrations are explicit and rare
- module schema migrations are explicit and local
- adding a module requires a default-state migration strategy for old saves
- removing or disabling a module requires a documented retention/cleanup policy

## Current migration seam behavior
- load currently runs through an explicit `SaveMigrationPipeline`
- same-version root and module schemas pass through unchanged
- root and module migrations can now be registered as explicit chained steps
- if no migration path is registered for a root or module schema jump, load fails explicitly with a migration error
- runtime debug snapshots, observability summaries, and latest-month diff traces remain outside the persisted compatibility surface
- runtime load-migration reports and hotspot summaries are explanatory overlays only; they do not extend root or module save namespaces
- runtime scale summaries, payload-footprint summaries, and migration-consistency warnings are also explanatory overlays only
- runtime domain-event targeting and cause metadata used by the event-handling seam is also non-persisted and does not extend save namespaces
- runtime event-contract health classifications used by ten-year diagnostics are also non-persisted and do not extend save namespaces
- migration preparation must not mutate the caller's source save root; consistency reporting happens on cloned preparation data only
- court-policy social-memory echo v125-v132 adds no persisted fields; it writes existing `MemoryRecordState` records with a new code constant and structured `CauseKey` only
- court-policy memory-pressure readback v133-v140 adds no persisted fields; it projects existing SocialMemory cause/type/weight and Office/PublicLife snapshots only
- court-policy public-reading echo v141-v148 adds no persisted fields; it projects existing SocialMemory cause/type/weight and Office/PublicLife snapshots onto governance/public-life command readbacks only
- court-policy public follow-up cue v149-v156 adds no persisted fields; it projects existing SocialMemory cause outcome data and PublicLife scalars onto governance/public-life command readbacks only
- court-policy follow-up docket guard v157-v164 adds no persisted fields; it projects existing SocialMemory cause outcome data and PublicLife scalars onto governance/docket no-loop guard readbacks only
- court-policy suggested action guard v165-v172 adds no persisted fields; it projects existing SocialMemory cause outcome data, PublicLife scalars, and already-selected affordance text onto governance suggested prompts and docket guidance only
- court-policy suggested receipt guard v173-v180 adds no persisted fields; it projects existing SocialMemory cause outcome data and PublicLife scalars onto command receipt readbacks only
- court-policy receipt-docket consistency guard v181-v188 adds no persisted fields; it projects existing SocialMemory cause outcome data and PublicLife scalars onto governance/docket no-loop guard readbacks only
- court-policy public-life receipt echo v189-v196 adds no persisted fields; it projects existing SocialMemory cause outcome data and PublicLife scalars onto public-life command readbacks only
- court-policy first rule-density closeout audit v197-v204 adds no persisted fields; it is documentation/test governance over the v109-v196 first-layer branch and does not create court process state, appointment slate, dispatch arrival, downstream household/market/public consequence state, or any ledger
- social mobility fidelity ring v213-v244 adds no persisted fields; it activates existing `PopulationAndHouseholds` schema `3` household, membership, labor-pool, marriage-pool, and migration-pool carriers plus existing `PersonRegistry` schema `1` `FidelityRing`. No root/module schema version changes, migration, save manifest change, social-mobility ledger, movement ledger, focus ledger, projection cache, manager/controller layer, or `PersonRegistry` domain expansion is introduced.
- regime legitimacy readback v253-v260 adds no persisted fields; it projects existing `WorldSettlements.RegimeLegitimacyShifted` runtime metadata, `OfficeAndCareer.OfficeDefected` runtime metadata, `JurisdictionAuthoritySnapshot`, and `SettlementPublicLifeSnapshot` into governance/public-life readbacks only. No root/module schema version changes, migration, save manifest change, full regime engine, faction AI, legitimacy ledger, defection ledger, owner-lane ledger, cooldown ledger, projection cache, manager/controller layer, same-month SocialMemory residue, or `PersonRegistry` expansion is introduced.
- regime legitimacy readback closeout v261-v268 adds no persisted fields; it remains docs/tests only over the v253-v260 first readback branch and does not create regime process state, public allegiance state, durable regime memory state, legitimacy/defection/owner-lane/cooldown/scheduler ledgers, projection cache, schema migration, manager/controller layer, same-month SocialMemory residue, or `PersonRegistry` expansion.

## Versioning rules
- root save uses one root version integer
- every module uses its own schema version integer
- incompatible module changes do not justify hidden root changes

## Current implemented module versions
- `PersonRegistry` uses namespace `PersonRegistry` with schema version `1`
- `WorldSettlements` uses namespace `WorldSettlements` with schema version `8` for the active world-settlement slice plus chain-6 flood-disaster and chain-5 frontier-strain declaration watermark state
- `FamilyCore` uses namespace `FamilyCore` with schema version `8`; v53-v60 Family-lane closure readback reads existing clan elder explanation, household guarantee, lineage-house face, sponsor pressure, and owner-response trace fields and adds no Family state
- `PopulationAndHouseholds` uses namespace `PopulationAndHouseholds` with schema version `3`
- `SocialMemoryAndRelations` uses namespace `SocialMemoryAndRelations` with schema version `3` for clan emotional climate, person pressure-tempering ledgers, public-life order accepted/refused/partial/response residue, later response-residue decay/hardening, v8 actor-countermove back-pressure inputs, v13 home-household local response residue, and v14 home-household repeat-friction inputs stored in existing memory/narrative/climate records. v15 common-household response texture through v30 owner-lane closure audit read existing fields/projections and add no SocialMemory state. V46-V52 Office-lane closure readback and v53-v60 Family-lane closure readback also read existing residue snapshots and add no SocialMemory state. V125-V132 court-policy social-memory echo writes existing memory/narrative/climate fields only and adds no schema. V133-V140 court-policy memory-pressure readback, v141-v148 court-policy public-reading echo, v149-v156 court-policy public follow-up cue, v157-v164 court-policy follow-up docket guard, v165-v172 court-policy suggested action guard, v173-v180 court-policy suggested receipt guard, v181-v188 court-policy receipt-docket consistency guard, and v189-v196 court-policy public-life receipt echo only project existing SocialMemory snapshots and add no schema. V197-V204 court-policy first rule-density closeout audit is docs/test governance only and adds no schema.
- `EducationAndExams` uses namespace `EducationAndExams` with schema version `2`
- `TradeAndIndustry` uses namespace `TradeAndIndustry` with schema version `4`
- `PublicLifeAndRumor` uses namespace `PublicLifeAndRumor` with schema version `4` for the active county-public-life slice plus monthly-cadence, venue-channel, and channel-contention descriptors. V253-V260 Chain 9 public readback updates existing public-life summary fields only and adds no schema.
- `OfficeAndCareer` uses namespace `OfficeAndCareer` with schema version `7` for the active governance-lite slice plus chain-4 amnesty de-duplication state, chain-7 clerk-capture edge state, chain-9 official-defection risk state, office-owned public-life refusal response trace state, v37 policy implementation drag over existing office fields, and v46-v52 Office-lane closure readback over existing office response / yamen fields. V253-V260 reads existing official-defection risk state only and adds no schema.
- `NarrativeProjection` uses namespace `NarrativeProjection` with schema version `1`
- `OrderAndBanditry` uses namespace `OrderAndBanditry` with schema version `9`
- `ConflictAndForce` uses namespace `ConflictAndForce` with schema version `4` for active M3 local-conflict lite integration plus campaign-fallout persistence
- `WarfareCampaign` uses namespace `WarfareCampaign` with schema version `4` for the active campaign-lite slice (phase + aftermath docket projection)

## M2-lite default-state policy
- old saves without `EducationAndExams` or `TradeAndIndustry` load cleanly when those modules remain disabled in the feature manifest
- old M0-M1 saves must continue to load through the M2 loader when `EducationAndExams`, `TradeAndIndustry`, and `NarrativeProjection` remain disabled in the manifest
- enabling either M2-lite module requires creating its owned default state inside its own namespace only
- built-in default loaders now migrate legacy `FamilyCore` schemas through `8` by first backfilling lineage-conflict defaults, then conservatively backfilling marriage/heir/mourning lifecycle defaults, then adding family-owned public-life refusal response trace fields inside the family namespace only
- built-in default loaders now also migrate legacy `WorldSettlements` schema `1 -> 2 -> 3 -> 4 -> 5 -> 6 -> 7 -> 8` inside the world-settlement namespace only; schema `7` backfills `LastDeclaredFloodDisasterBand = 0`, and schema `8` backfills `LastDeclaredFrontierStrainBand = 0` so old saves do not repeat a past flood or frontier declaration after load
- `PublicLifeAndRumor` now defaults to schema `4` when enabled and owns its public-pulse plus monthly-cadence / venue-channel / channel-contention state inside its own namespace only
- old saves still load cleanly while `PublicLifeAndRumor` remains disabled in the manifest
- active M2 and later bootstrap paths may now enable `PublicLifeAndRumor`; that new envelope is intentional and documented rather than implicit schema drift
- built-in default loaders now migrate legacy `PublicLifeAndRumor` schema `1 -> 2 -> 3 -> 4` by first backfilling cadence code/label, crowd mix, and cadence summary, then by conservatively backfilling dominant-venue code plus channel metrics, then by backfilling official/street/road/prefecture/contention wording inside the public-life namespace only
- built-in default loaders now also migrate legacy `TradeAndIndustry` schema `1 -> 2 -> 3` by first backfilling gray-route ledgers, then by conservatively backfilling per-route blockage / seizure mirrors inside the same trade namespace only
- built-in default loaders now also migrate legacy `SocialMemoryAndRelations` schema `1 -> 2 -> 3` by first classifying legacy memory records, then by backfilling clan emotional climate records from existing clan narratives inside the same social-memory namespace only
- built-in default loaders now also migrate legacy `PopulationAndHouseholds` schema `2 -> 3` by initializing home-household local response traces inside the population namespace only
- public-life order residue v4/v5/v6/v7/v8/v10/v11/v12/v13/v14/v15/v16/v17/v18/v19/v20/v21/v22/v23/v24/v25/v26/v27/v28/v29/v30 does not add a new SocialMemory field or module envelope; it writes, adjusts, reads, or projects records inside existing schema `3` collections and runtime read-models, and therefore requires no `3 -> 4` migration. v20-v30 owner-lane return, entry, status, outcome, residue, follow-up, affordance echo, receipt closure, no-loop guard, and audit wording are runtime projections over existing `HouseholdPressureSnapshot`, owner-module `LastRefusalResponse*` fields, and `SocialMemoryEntrySnapshot.CauseKey` / `State` / `Weight` / `OriginDate`. They add no cooldown ledger, repeated-response counter, receipt-status ledger, outcome ledger, follow-up ledger, stale-guidance ledger, SocialMemory field, index, namespace, command target field, or migration. Any later residue work that adds SocialMemory fields, household authority fields outside `PopulationAndHouseholds`, indexes, command target fields, or namespaces must bump the owning module schema and add an explicit migration.
- M2-lite explanation strings are module-owned authoritative traces and must roundtrip with the rest of the module state
- `NarrativeProjection` persists derived notification history only inside its own namespace and may be rebuilt later without rewriting foreign module state
- latest-month debug traces, warning lists, and module inspectors are non-persisted read models and must not require a root schema change
- explicit schema mismatches must fail load clearly instead of silently coercing incompatible module envelopes

## M3 local-conflict namespace policy
- `OrderAndBanditry` now has an order-enabled M3 bridge path and a conflict-enabled M3 local-conflict path; both seed module-owned settlement disorder state only when the feature is enabled
- old M2 saves still load cleanly while `OrderAndBanditry` remains disabled in the manifest
- legacy M3 order saves with `OrderAndBanditry` schema `1` now migrate through built-in `1 -> 2 -> 3 -> 4 -> 5 -> 6 -> 7 -> 8 -> 9` steps that first backfill black-route pressure summaries, then conservatively reconstruct paper-compliance and implementation-drag fields, then backfill route-shielding and retaliation-risk summaries, then backfill empty intervention-receipt fields, then clamp one-month intervention-follow-through state, then backfill structured outcome/refusal/partial trace fields, then add structured refusal-response trace fields inside the same namespace
- public-life order residue v4 adds only query/read-model exposure for existing order aftermath fields; v5 adds `OrderAndBanditry` persisted structured trace fields (`LastInterventionOutcomeCode`, `LastInterventionRefusalCode`, `LastInterventionPartialCode`, `LastInterventionTraceCode`, `RefusalCarryoverMonths`) and a same-namespace `7 -> 8` migration
- public-life order response v6 adds `OrderAndBanditry` persisted response fields (`LastRefusalResponseCommandCode`, `LastRefusalResponseCommandLabel`, `LastRefusalResponseSummary`, `LastRefusalResponseOutcomeCode`, `LastRefusalResponseTraceCode`, `ResponseCarryoverMonths`) and a same-namespace `8 -> 9` migration for order-owned road watch / route-pressure repair responses
- public-life order actor countermove v8 adds trace-code constants only. `OrderAndBanditry` remains schema `9`, `OfficeAndCareer` remains schema `7`, `FamilyCore` remains schema `8`, and `SocialMemoryAndRelations` remains schema `3`; the countermoves reuse the v6 response fields and existing SocialMemory records.
- public-life order ordinary-household readback/play-surface v10/v11 adds runtime read-model constants / projection enrichment only. v12 adds `PopulationAndHouseholds` persisted local response trace fields (`LastLocalResponseCommandCode`, `LastLocalResponseCommandLabel`, `LastLocalResponseOutcomeCode`, `LastLocalResponseTraceCode`, `LastLocalResponseSummary`, `LocalResponseCarryoverMonths`) and a same-namespace `2 -> 3` migration for home-household local commands. `OrderAndBanditry` remains schema `9`, `OfficeAndCareer` remains schema `7`, `FamilyCore` remains schema `8`, and `SocialMemoryAndRelations` remains schema `3`.
- public-life order home-household social-memory readback v13 adds no persisted fields: `SocialMemoryAndRelations` remains schema `3` and reads structured `PopulationAndHouseholds` local response aftermath through queries, while `PopulationAndHouseholds` remains schema `3`.
- public-life order home-household repeat friction v14 adds no persisted fields: `PopulationAndHouseholds` remains schema `3` and reads existing SocialMemory snapshots through queries, while `SocialMemoryAndRelations` remains schema `3`.
- public-life order common-household response texture v15 adds no persisted fields: `PopulationAndHouseholds` remains schema `3` and derives texture from existing household fields, while `SocialMemoryAndRelations` remains schema `3`.
- public-life order home-household response capacity v16 adds no persisted fields: `PopulationAndHouseholds` remains schema `3` and derives capacity from existing household fields, while `SocialMemoryAndRelations` remains schema `3`.
- public-life order home-household response tradeoff forecast v17 adds no persisted fields: `PopulationAndHouseholds` remains schema `3` and derives tradeoff forecast from existing household fields, while `SocialMemoryAndRelations` remains schema `3`.
- public-life order home-household short-term readback v18 adds no persisted fields: `PopulationAndHouseholds` remains schema `3` and derives receipt readback from existing household fields and structured local response codes, while `SocialMemoryAndRelations` remains schema `3`.
- public-life order home-household follow-up affordance v19 adds no persisted fields: `PopulationAndHouseholds` remains schema `3` and derives follow-up hints from existing household fields and structured local response codes, while `SocialMemoryAndRelations` remains schema `3`.
- public-life order owner-lane return guidance v20 adds no persisted fields: `PopulationAndHouseholds` remains schema `3` and derives owner-lane return guidance from existing household fields and structured local response codes, while `SocialMemoryAndRelations` remains schema `3`.
- public-life order owner-lane surface readback v21 adds no persisted fields: Office/Governance and Family-facing owner-lane guidance is runtime projection over existing household local response fields; `PopulationAndHouseholds`, `OrderAndBanditry`, `OfficeAndCareer`, `FamilyCore`, and `SocialMemoryAndRelations` schema versions remain unchanged.
- public-life order owner-lane handoff entry readback v22 adds no persisted fields: `承接入口` labels are runtime projection text over existing owner-lane command affordances and structured household local response fields; all module schema versions remain unchanged.
- public-life order owner-lane receipt status readback v23 adds no persisted fields: `归口状态` text is runtime projection over existing household local response fields and existing `OrderAndBanditry` / `OfficeAndCareer` / `FamilyCore` structured response trace fields; all module schema versions remain unchanged.
- public-life order owner-lane outcome reading guidance v24 adds no persisted fields: `归口后读法` text is runtime projection over existing owner-lane `LastRefusalResponseOutcomeCode` values plus existing household local response context; all module schema versions remain unchanged and no migration is required.
- public-life order owner-lane social-residue readback v25 adds no persisted fields: `社会余味读回` text is runtime projection over existing SocialMemory response cause keys, state, weight, origin date, and owner-lane response traces. SocialMemory same-month de-duplication now preserves distinct response residues by `CauseKey` as well as memory kind, but all module schema versions remain unchanged and no migration is required.
- public-life order owner-lane social-residue follow-up guidance v26 adds no persisted fields: `余味冷却提示` / `余味续接提示` / `余味换招提示` text is runtime projection over existing SocialMemory response cause keys, state, weight, origin date, and owner-lane response outcome traces; all module schema versions remain unchanged and no migration is required.
- public-life order owner-lane closure v27-v30 adds no persisted fields: `现有入口读法`, `后手收口读回`, and `闭环防回压` text is runtime projection over existing SocialMemory response cause keys, state, weight, origin date, and owner-lane response outcome traces; all module schema versions remain unchanged and no migration is required.
- backend event contract health v32 adds no persisted fields: diagnostic classifications for `DomainEvent` contract debt are runtime/test evidence only, and all module schema versions remain unchanged with no migration required.
- backend event contract health v33 adds no persisted fields: the no-unclassified event-contract gate is an integration diagnostic assertion over runtime event counts and module declarations only. It adds no module envelope, root/module schema version change, migration, save roundtrip behavior, event ledger, or owner-lane registry.
- backend event contract health v34 adds no persisted fields: diagnostic owner/evidence backlinks are formatted from structured event keys and classification kinds after runtime diagnostics run. They add no module envelope, root/module schema version change, migration, save roundtrip behavior, event-health ledger, or owner-lane registry.
- backend canal-window Trade/Order hookup v35 adds no persisted fields: new canal-window metadata keys are runtime `DomainEvent` payload only, and the Trade / Order consumers reuse existing `TradeAndIndustry` schema `4`, `OrderAndBanditry` schema `9`, and `WorldSettlements` schema `8` fields. No root/module schema version changes, migration, save roundtrip change, canal ledger, or owner-lane ledger are required.
- backend household-family burden v36 adds no persisted fields: new household/family pressure metadata keys are runtime `DomainEvent` payload only, and the Family consumer reuses existing `FamilyCore` schema `8` fields after reading existing `PopulationAndHouseholds` schema `3` household snapshots. No root/module schema version change, migration, save roundtrip change, relief ledger, sponsor-lane ledger, household target field, or SocialMemory field is required.
- backend office/yamen implementation drag v37 adds no persisted fields: new `PolicyImplementation*` metadata keys are runtime `DomainEvent` payload only, and the Office consumer reuses existing `OfficeAndCareer` schema `7` task/backlog/clerk/leverage/petition fields. No root/module schema version change, migration, save roundtrip change, policy ledger, yamen workflow state, owner-lane ledger, or SocialMemory field is required.
- backend office/yamen readback v38-v45 adds no persisted fields: public-life readback consumes structured runtime event metadata, governance/player-command fields are runtime projections, Unity fields are ViewModel copies, and later SocialMemory office-policy residue reuses existing schema `3` memory/narrative/climate records. `OfficeAndCareer` remains schema `7`, `PublicLifeAndRumor` remains schema `4`, and `SocialMemoryAndRelations` remains schema `3`; no root/module schema version change, migration, save roundtrip change, policy ledger, yamen workflow state, owner-lane ledger, cooldown ledger, household target field, projection cache, or save manifest update is required.
- backend office-lane closure v46-v52 adds no persisted fields: `OfficeLaneEntryReadbackSummary`, `OfficeLaneReceiptClosureSummary`, `OfficeLaneResidueFollowUpSummary`, and `OfficeLaneNoLoopGuardSummary` are runtime read-model / ViewModel projection fields over existing `OfficeAndCareer` snapshots, owner-response traces, and `SocialMemoryEntrySnapshot` cause keys. `OfficeAndCareer` remains schema `7`, `PublicLifeAndRumor` remains schema `4`, and `SocialMemoryAndRelations` remains schema `3`; no root/module schema version change, migration, save roundtrip change, policy ledger, yamen workflow state, owner-lane ledger, receipt-status ledger, outcome ledger, cooldown ledger, follow-up ledger, household target field, projection cache, or save manifest update is required.
- backend Family-lane closure v53-v60 adds no persisted fields: `FamilyLaneEntryReadbackSummary`, `FamilyElderExplanationReadbackSummary`, `FamilyGuaranteeReadbackSummary`, `FamilyHouseFaceReadbackSummary`, `FamilyLaneReceiptClosureSummary`, `FamilyLaneResidueFollowUpSummary`, and `FamilyLaneNoLoopGuardSummary` are runtime read-model / ViewModel projection fields over existing `FamilyCore` snapshots, sponsor-clan household reads, Family owner-response traces, and `SocialMemoryEntrySnapshot` cause keys. `FamilyCore` remains schema `8`, `PopulationAndHouseholds` remains schema `3`, and `SocialMemoryAndRelations` remains schema `3`; no root/module schema version change, migration, save roundtrip change, Family closure ledger, guarantee ledger, owner-lane ledger, receipt-status ledger, outcome ledger, cooldown ledger, follow-up ledger, household target field, projection cache, or save manifest update is required.
- family relief choice v61-v68 adds no persisted fields: `GrantClanRelief` reuses existing `FamilyCore` schema `8` fields (`CharityObligation`, `SupportReserve`, `BranchTension`, `BranchFavorPressure`, `ReliefSanctionPressure`, `MediationMomentum`, and existing conflict receipt fields), while `PopulationAndHouseholds` remains schema `3` and `SocialMemoryAndRelations` remains schema `3`. No root/module schema version change, migration, save roundtrip change, relief ledger, charity ledger, guarantee ledger, Family closure ledger, owner-lane ledger, cooldown ledger, household target field, projection cache, or save manifest update is required.
- `ConflictAndForce` now has a conflict-enabled M3 local-conflict path and seeds module-owned settlement force posture plus explicit response activation/support fields only when the feature is enabled
- legacy M3 local-conflict saves with `ConflictAndForce` schema `1` now migrate through a built-in `1 -> 2` module step during default local-conflict load
- built-in migration now also upgrades legacy `ConflictAndForce` schema `2` saves to schema `3` by backfilling zero campaign-fatigue / escort-strain fields and empty fallout traces inside the same namespace
- no existing save gains `OrderAndBanditry` or `ConflictAndForce` envelopes implicitly unless the feature manifest enables them

## Governance-lite namespace policy
- `OfficeAndCareer` now has a dedicated governance-lite path that seeds its own office-career and jurisdiction-authority state only when the feature is enabled
- legacy governance-lite saves with `OfficeAndCareer` schema `1` now migrate through built-in `1 -> 2 -> 3 -> 4 -> 5 -> 6 -> 7` module steps during default governance-lite load; `6 -> 7` adds office-owned public-life refusal response trace fields for yamen/document/clerk-delay handling
- old M2 and M3 saves still load cleanly while `OfficeAndCareer` remains disabled in their manifests
- no existing stable M2/M3 path gains an `OfficeAndCareer` envelope implicitly; only the governance-lite path does

## Campaign-lite namespace policy
- `WarfareCampaign` now has a dedicated campaign-enabled path that seeds its own campaign-board and mobilization-signal state only when the feature is enabled
- old M2, M3, and governance-lite saves still load cleanly while `WarfareCampaign` remains disabled in their manifests
- no existing stable path gains a `WarfareCampaign` envelope implicitly; only the campaign-enabled path does
- built-in migration now upgrades legacy campaign-enabled saves from schema `1` to `2` to `3` by reconstructing board labels, command-fit wording, commander summaries, bounded route descriptors, and directive descriptors inside the same namespace
- force/campaign/regime owner-lane readback v69-v76 adds no persisted fields: `WarfareLaneEntryReadbackSummary`, `ForceReadinessReadbackSummary`, `CampaignAftermathReadbackSummary`, `WarfareLaneReceiptClosureSummary`, `WarfareLaneResidueFollowUpSummary`, and `WarfareLaneNoLoopGuardSummary` are runtime read-model / ViewModel fields over existing `ConflictAndForce`, `WarfareCampaign`, `OfficeAndCareer`, and `SocialMemoryAndRelations` snapshots. No root/module schema version change, migration, save roundtrip change, force/campaign closure ledger, owner-lane ledger, cooldown ledger, household target field, projection cache, or save manifest update is required.
- warfare directive choice v77-v84 adds no persisted fields: `军令选择读回`, `案头筹议选择`, `点兵加压选择`, `粮道护持选择`, `归营止损选择`, `WarfareCampaign拥有军令`, and `军务选择不是县门文移代打` reuse existing `WarfareCampaign` directive fields and runtime command/readback DTO strings. No root/module schema version change, migration, save roundtrip change, directive ledger, force/campaign closure ledger, owner-lane ledger, cooldown ledger, household target field, projection cache, or save manifest update is required.
- warfare aftermath docket readback v85-v92 adds no persisted fields: `战后案卷读回`, `记功簿读回`, `劾责状读回`, `抚恤簿读回`, `清路札读回`, `WarfareCampaign拥有战后案卷`, `战后案卷不是县门/Order代算`, `不是普通家户补战后`, and `军务案卷防回压` reuse existing `WarfareCampaign` schema `4` aftermath docket fields and runtime read-model DTO strings. No root/module schema version change, migration, save roundtrip change, aftermath ledger, relief ledger, route-repair ledger, force/campaign closure ledger, owner-lane ledger, cooldown ledger, household target field, projection cache, or save manifest update is required.
- court-policy process readback v93-v100 adds no persisted fields: `CourtPolicyEntryReadbackSummary`, `CourtPolicyDispatchReadbackSummary`, `CourtPolicyPublicReadbackSummary`, and `CourtPolicyNoLoopGuardSummary` are runtime read-model / ViewModel projection fields over existing `OfficeAndCareer` schema `7` and `PublicLifeAndRumor` schema `4` snapshots. No root/module schema version change, migration, save roundtrip change, court module, dispatch ledger, policy closure ledger, owner-lane ledger, cooldown ledger, household target field, projection cache, or save manifest update is required.
- thin-chain closeout audit v101-v108 adds no persisted fields: it is documentation plus architecture-test governance over the v3-v100 thin-chain evidence. No root/module schema version change, migration, save roundtrip change, module envelope, manifest change, ledger, household target field, projection cache, or serialized payload change is required.
- court-policy process thickening v109-v116 adds no persisted fields: `政策语气读回`, `文移指向读回`, `县门承接姿态`, `公议承压读法`, `朝廷后手仍不直写地方`, and `不是本户硬扛朝廷后账` are runtime projection / owner-lane readback strings over existing `OfficeAndCareer` schema `7`, `PublicLifeAndRumor` schema `4`, `WorldSettlements` schema `8`, and `SocialMemoryAndRelations` schema `3` contracts. No root/module schema version change, migration, save roundtrip change, Court module, dispatch ledger, policy ledger, court-process ledger, owner-lane ledger, cooldown ledger, household target field, projection cache, or save manifest update is required.
- court-policy local response v117-v124 adds no persisted fields: `政策回应入口`, `文移续接选择`, `县门轻催`, `递报改道`, `公议降温只读回`, and `不是本户硬扛朝廷后账` are runtime command/readback projection strings over existing `OfficeAndCareer` schema `7`, `PublicLifeAndRumor` schema `4`, `WorldSettlements` schema `8`, and `SocialMemoryAndRelations` schema `3` contracts. `PressCountyYamenDocument`, `RedirectRoadReport`, `PostCountyNotice`, and `DispatchRoadReport` reuse existing command/state paths. No root/module schema version change, migration, save roundtrip change, Court module, dispatch ledger, policy ledger, court-process ledger, owner-lane ledger, cooldown ledger, household target field, projection cache, or save manifest update is required.
- court-policy public-reading echo v141-v148 adds no persisted fields: `政策公议旧读回`, `公议旧账回声`, `下一次榜示/递报旧读法`, `PublicLife只读街面解释`, and `县门承接仍归OfficeAndCareer` are runtime command/readback projection strings over existing `OfficeAndCareer` schema `7`, `PublicLifeAndRumor` schema `4`, `WorldSettlements` schema `8`, and `SocialMemoryAndRelations` schema `3` contracts. No root/module schema version change, migration, save roundtrip change, Court module, dispatch ledger, policy ledger, court-process ledger, owner-lane ledger, cooldown ledger, memory-pressure ledger, public-reading ledger, household target field, projection cache, or save manifest update is required.
- court-policy public follow-up cue v149-v156 adds no persisted fields: `政策公议后手提示`, `公议冷却提示`, `公议轻续提示`, `公议换招提示`, `下一步仍看榜示/递报承口`, and `不是冷却账本` are runtime command/readback projection strings over existing `OfficeAndCareer` schema `7`, `PublicLifeAndRumor` schema `4`, `WorldSettlements` schema `8`, and `SocialMemoryAndRelations` schema `3` contracts. No root/module schema version change, migration, save roundtrip change, Court module, dispatch ledger, policy ledger, court-process ledger, owner-lane ledger, cooldown ledger, memory-pressure ledger, public-reading ledger, public-follow-up ledger, household target field, projection cache, or save manifest update is required.
- court-policy follow-up docket guard v157-v164 adds no persisted fields: `政策后手案牍防误读`, `公议后手只作案牍提示`, `不是Order后账`, `不是Office成败`, and `仍等Office/PublicLife/SocialMemory分读` are runtime governance/docket projection strings over existing `OfficeAndCareer` schema `7`, `PublicLifeAndRumor` schema `4`, `WorldSettlements` schema `8`, and `SocialMemoryAndRelations` schema `3` contracts. No root/module schema version change, migration, save roundtrip change, Court module, dispatch ledger, policy ledger, court-process ledger, owner-lane ledger, cooldown ledger, memory-pressure ledger, public-reading ledger, public-follow-up ledger, docket ledger, household target field, projection cache, or save manifest update is required.
- court-policy receipt-docket consistency guard v181-v188 adds no persisted fields: `回执案牍一致防误读`, `回执只回收已投影的政策公议后手`, and `案牍不把回执读成新政策结果` are runtime governance/docket projection strings over existing `OfficeAndCareer` schema `7`, `PublicLifeAndRumor` schema `4`, `WorldSettlements` schema `8`, and `SocialMemoryAndRelations` schema `3` contracts. No root/module schema version change, migration, save roundtrip change, Court module, dispatch ledger, policy ledger, court-process ledger, owner-lane ledger, cooldown ledger, memory-pressure ledger, public-reading ledger, public-follow-up ledger, docket ledger, receipt ledger, receipt-docket ledger, household target field, projection cache, or save manifest update is required.

- court-policy public-life receipt echo v189-v196 adds no persisted fields: `公议回执回声防误读`, `街面只读已投影的政策公议后手`, and `公议不把回执读成新政令` are runtime public-life command projection strings over existing `OfficeAndCareer` schema `7`, `PublicLifeAndRumor` schema `4`, `WorldSettlements` schema `8`, and `SocialMemoryAndRelations` schema `3` contracts. No root/module schema version change, migration, save roundtrip change, Court module, dispatch ledger, policy ledger, court-process ledger, owner-lane ledger, cooldown ledger, memory-pressure ledger, public-reading ledger, public-follow-up ledger, docket ledger, receipt ledger, receipt-docket ledger, public-life receipt echo ledger, household target field, projection cache, or save manifest update is required.

## Post-MVP preflight namespace policy
- social mobility fidelity ring v213-v244 remains schema-neutral: `FidelityScaleSnapshot`, `SettlementMobilitySnapshot`, person dossier movement/fidelity readbacks, runtime observability counts, and Unity shell mobility strings are runtime projections or diagnostics only. `PopulationAndHouseholds` remains schema `3`; `PersonRegistry` remains schema `1`; `SocialMemoryAndRelations` remains later durable-residue only. No migration is required.
- social mobility fidelity ring closeout v245-v252 remains docs/tests only: it adds no module namespace, root/module schema version change, migration, save-manifest membership, movement ledger, social-mobility ledger, focus ledger, owner-lane ledger, scheduler ledger, projection cache, dormant-stub store, or `PersonRegistry` domain expansion.
- social mobility scale budget guard v269-v276 remains docs/tests only: it adds no module namespace, root/module schema version change, migration, save-manifest membership, movement ledger, social-mobility ledger, focus ledger, scheduler ledger, global person simulation manager, projection cache, dormant-stub store, or `PersonRegistry` domain expansion.
- social mobility influence readback v277-v284 remains read-model/ViewModel only: `InfluenceFootprintReadbackSummary` and `ScaleBudgetReadbackSummary` are runtime projection fields, not module namespaces, saved state, migration inputs, movement/social/focus/scheduler ledgers, projection caches, dormant-stub stores, or `PersonRegistry` domain expansion.
- social mobility boundary closeout v285-v292 remains docs/tests only: it adds no module namespace, root/module schema version change, migration, save-manifest membership, movement/social/focus/scheduler/command/personnel ledger, projection cache, dormant-stub store, durable movement-residue state, global person simulation manager, or `PersonRegistry` domain expansion.
- personnel command preflight v293-v300 remains docs/tests only: it adds no module namespace, root/module schema version change, migration, save-manifest membership, command/movement/personnel/assignment/focus/scheduler ledger, projection cache, direct move/transfer/summon/assign-person state, durable movement-residue state, global person simulation manager, or `PersonRegistry` domain expansion.
- personnel flow command readiness v301-v308 remains read-model/ViewModel only: `PersonnelFlowReadinessSummary` is runtime projection text on existing player-command affordances/receipts. It adds no module namespace, root/module schema version change, migration, save-manifest membership, command/movement/personnel/assignment/focus/scheduler ledger, projection cache, direct move/transfer/summon/assign-person state, durable movement-residue state, global person simulation manager, or `PersonRegistry` domain expansion.
- personnel flow surface echo v309-v316 remains read-model/ViewModel only: `PlayerCommandSurfaceSnapshot.PersonnelFlowReadinessSummary` is runtime projection text assembled from existing command readiness fields. It adds no module namespace, root/module schema version change, migration, save-manifest membership, command/movement/personnel/assignment/focus/scheduler/surface-echo ledger, projection cache, direct move/transfer/summon/assign-person state, durable movement-residue state, global person simulation manager, or `PersonRegistry` domain expansion.
- personnel flow readiness closeout v317-v324 is docs/tests only: it records v293-v316 as a first command-readiness layer and adds no module namespace, root/module schema version change, migration, save-manifest membership, command/movement/personnel/assignment/focus/scheduler/closeout ledger, projection cache, direct move/transfer/summon/assign-person state, durable movement-residue state, global person simulation manager, or `PersonRegistry` domain expansion.
- personnel flow owner-lane gate v325-v332 remains read-model/ViewModel only: `PlayerCommandSurfaceSnapshot.PersonnelFlowOwnerLaneGateSummary` is runtime projection text assembled from existing command affordance/receipt metadata. It adds no module namespace, root/module schema version change, migration, save-manifest membership, command/movement/personnel/assignment/focus/scheduler/owner-lane-gate ledger, projection cache, direct move/transfer/summon/assign-person state, durable movement-residue state, global person simulation manager, or `PersonRegistry` domain expansion.
- personnel flow desk gate echo v333-v340 remains presentation-only: Desk Sandbox may display `PlayerCommandSurfaceSnapshot.PersonnelFlowOwnerLaneGateSummary` when local structured command readiness exists. It adds no module namespace, root/module schema version change, migration, save-manifest membership, command/movement/personnel/assignment/focus/scheduler/desk-gate ledger, projection cache, direct move/transfer/summon/assign-person state, durable movement-residue state, global person simulation manager, or `PersonRegistry` domain expansion.
- personnel flow desk gate containment v341-v348 adds no persisted fields: it adds only tests/docs and a negative presentation proof that quiet settlements do not inherit the local desk gate echo. It adds no module namespace, root/module schema version change, migration, save-manifest membership, command/movement/personnel/assignment/focus/scheduler/desk-gate-containment ledger, projection cache, direct move/transfer/summon/assign-person state, durable movement-residue state, global person simulation manager, or `PersonRegistry` domain expansion.
- personnel flow gate closeout v349-v356 is docs/tests only: it closes v325-v348 as a readback layer and adds no module namespace, root/module schema version change, migration, save-manifest membership, command/movement/personnel/assignment/focus/scheduler/owner-lane-gate ledger, projection cache, direct move/transfer/summon/assign-person state, durable movement-residue state, global person simulation manager, or `PersonRegistry` domain expansion.
- personnel flow future owner-lane preflight v357-v364 is docs/tests only: future Family/Office/Warfare personnel-flow owner lanes still require separate schema/migration review before persisted state, command history, movement receipt, assignment state, migration state, durable residue, projection cache, or save-manifest membership is added.
- personnel flow future lane surface v365-v372 adds no persisted fields: `PlayerCommandSurfaceSnapshot.PersonnelFlowFutureOwnerLanePreflightSummary` is runtime read-model text only and adds no module namespace, root/module schema version change, migration, save-manifest membership, command/movement/personnel/assignment/focus/scheduler/future-lane-surface ledger, projection cache, direct move/transfer/summon/assign-person state, durable movement-residue state, global person simulation manager, or `PersonRegistry` domain expansion.
- personnel flow future lane closeout v373-v380 adds no persisted fields: it is docs/tests closeout only and adds no module namespace, root/module schema version change, migration, save-manifest membership, command/movement/personnel/assignment/focus/scheduler/future-lane-closeout ledger, projection cache, direct move/transfer/summon/assign-person state, durable movement-residue state, global person simulation manager, or `PersonRegistry` domain expansion.
- commoner social position preflight v381-v388 adds no persisted fields: it is docs/tests preflight only and adds no module namespace, root/module schema version change, migration, save-manifest membership, promote/demote state, zhuhu/kehu conversion state, class/social-position/personnel/movement/focus/scheduler ledger, projection cache, durable social-position residue, global per-person career simulation manager, or `PersonRegistry` domain expansion.
- commoner social position readback v389-v396 adds no persisted fields: `PersonDossierSnapshot.SocialPositionReadbackSummary` and `PersonDossierViewModel.SocialPositionReadbackSummary` are runtime read-model/ViewModel fields only and add no module namespace, root/module schema version change, migration, save-manifest membership, promote/demote state, zhuhu/kehu conversion state, class/social-position/personnel/movement/focus/scheduler/readback ledger, projection cache, durable social-position residue, or `PersonRegistry` domain expansion.
- social position owner-lane keys v397-v404 adds no persisted fields: `PersonDossierSnapshot.SocialPositionSourceModuleKeys` and `PersonDossierViewModel.SocialPositionSourceModuleKeys` are runtime read-model/ViewModel fields only and add no module namespace, root/module schema version change, migration, save-manifest membership, promote/demote state, zhuhu/kehu conversion state, class/social-position/personnel/movement/source-key ledger, projection cache, durable social-position residue, or `PersonRegistry` domain expansion.
- social position readback closeout v405-v412 adds no persisted fields: it is docs/tests closeout only and adds no module namespace, root/module schema version change, migration, save-manifest membership, promote/demote state, zhuhu/kehu conversion state, class/social-position/personnel/movement/closeout ledger, projection cache, durable social-position residue, or `PersonRegistry` domain expansion.
- social position scale budget v413-v420 adds no persisted fields: `PersonDossierSnapshot.SocialPositionScaleBudgetReadbackSummary` and `PersonDossierViewModel.SocialPositionScaleBudgetReadbackSummary` are runtime read-model/ViewModel fields only and add no module namespace, root/module schema version change, migration, save-manifest membership, promote/demote state, zhuhu/kehu conversion state, precision-band state, class/social-position/personnel/movement/scale-budget ledger, projection cache, durable social-position residue, or `PersonRegistry` domain expansion.
- social position regional scale guard v421-v428 adds no persisted fields: it is tests/docs guard only and adds no module namespace, root/module schema version change, migration, save-manifest membership, promote/demote state, zhuhu/kehu conversion state, precision-band state, class/social-position/personnel/movement/scale-budget ledger, projection cache, durable social-position residue, or `PersonRegistry` domain expansion.
- social position scale closeout v429-v436 adds no persisted fields: it is docs/tests closeout only and adds no module namespace, root/module schema version change, migration, save-manifest membership, promote/demote state, zhuhu/kehu conversion state, precision-band state, class/social-position/personnel/movement/source-key/scale-budget/closeout ledger, projection cache, durable social-position residue, or `PersonRegistry` domain expansion.
- commoner status owner-lane preflight v437-v444 adds no persisted fields: it is docs/tests preflight only and adds no module namespace, root/module schema version change, migration, save-manifest membership, promote/demote state, zhuhu/kehu conversion state, precision-band state, class/social-position/commoner-status/personnel/movement/owner-lane/preflight ledger, projection cache, durable social-position residue, or `PersonRegistry` domain expansion.
- fidelity scale budget preflight v445-v452 adds no persisted fields: it is docs/tests preflight only and adds no module namespace, root/module schema version change, migration, save-manifest membership, precision-band state, selector state, target-cardinality state, class/social-position/commoner-status/personnel/movement/scale-budget/fidelity-budget ledger, projection cache, durable residue, global person scanner, or `PersonRegistry` domain expansion.
- household mobility dynamics explanation v453-v460 adds no persisted fields: `MobilityDynamicsExplanationSummary`, `MobilityDynamicsDimensionKeys`, and `HouseholdMobilityDynamicsSummary` are runtime read-model/ViewModel fields only and add no module namespace, root/module schema version change, migration, save-manifest membership, selector state, target-cardinality state, class/commoner-status/personnel/movement/household-mobility ledger, projection cache, durable residue, or `PersonRegistry` domain expansion.
- household mobility dynamics closeout v461-v468 remains docs/tests only: it adds no module namespace, root/module schema version change, migration, save-manifest membership, selector state, target-cardinality state, route-history state, class/commoner-status/personnel/movement/household-mobility/closeout ledger, projection cache, durable residue, global person scanner, or `PersonRegistry` domain expansion.
- household mobility owner-lane preflight v469-v476 adds no persisted fields: it is docs/tests preflight only and adds no module namespace, root/module schema version change, migration, save-manifest membership, owner-lane state, selector state, target-cardinality state, route-history state, class/commoner-status/personnel/movement/household-mobility/preflight ledger, projection cache, durable residue, global person scanner, or `PersonRegistry` domain expansion.
- household mobility preflight closeout v485-v492 remains docs/tests only: it adds no module namespace, root/module schema version change, migration, save-manifest membership, owner-lane state, selector state, target-cardinality state, route-history state, class/commoner-status/personnel/movement/household-mobility/preflight/closeout ledger, projection cache, durable residue, global person scanner, or `PersonRegistry` domain expansion.
- household mobility runtime rules-data readiness v501-v508 adds no persisted fields: it is docs/tests readiness only and adds no module namespace, root/module schema version change, migration, save-manifest membership, rules-data file, rules-data loader, owner-lane state, selector state, target-cardinality state, route-history state, class/commoner-status/personnel/movement/household-mobility/runtime-rule/readiness ledger, projection cache, durable residue, global person scanner, or `PersonRegistry` domain expansion.
- household mobility rules-data contract preflight v509-v516 adds no persisted fields: it is docs/tests contract preflight only and adds no module namespace, root/module schema version change, migration, save-manifest membership, rules-data file, rules-data loader, validator, default config, content/config namespace, owner-lane state, selector state, target-cardinality state, route-history state, class/commoner-status/personnel/movement/household-mobility/rules-data/contract ledger, projection cache, durable residue, global person scanner, runtime plugin marketplace, arbitrary script surface, reflection-heavy loader, or `PersonRegistry` domain expansion.
- household mobility default rules-data skeleton v517-v524 adds no persisted fields: it is docs/tests skeleton-contract only and adds no module namespace, root/module schema version change, migration, save-manifest membership, rules-data file, rules-data loader, validator, default skeleton file, content/rules-data namespace, owner-lane state, selector state, target-cardinality state, route-history state, class/commoner-status/personnel/movement/household-mobility/rules-data/default-skeleton ledger, projection cache, durable residue, global person scanner, runtime plugin marketplace, arbitrary script surface, reflection-heavy loader, or `PersonRegistry` domain expansion.
- population households first hardcoded rule extraction v525-v532 adds no persisted fields: `PopulationHouseholdMobilityRulesData` is runtime owner-consumed code shape only and adds no module namespace, root/module schema version change, migration, save-manifest membership, rules-data file, rules-data loader, default config, content/rules-data namespace, owner-lane state, selector state, target-cardinality state, route-history state, class/commoner-status/personnel/movement/household-mobility/rules-data/extraction ledger, projection cache, durable residue, global person scanner, runtime plugin marketplace, arbitrary script surface, reflection-heavy loader, or `PersonRegistry` domain expansion.
- population households first mobility runtime rule v533-v540 adds no persisted fields: the monthly owner rule uses existing schema 3 `PopulationAndHouseholds` fields and adds no module namespace, root/module schema version change, migration, save-manifest membership, rules-data file, rules-data loader, default config, content/rules-data namespace, owner-lane state, selector state, target-cardinality state, route-history state, cooldown state, class/commoner-status/personnel/movement/household-mobility/runtime-rule ledger, projection cache, durable residue, global person scanner, runtime plugin marketplace, arbitrary script surface, reflection-heavy loader, or `PersonRegistry` domain expansion.
- household mobility first runtime rule closeout v541-v548 adds no persisted fields: it is docs/tests closeout only and adds no module namespace, root/module schema version change, migration, save-manifest membership, rules-data file, rules-data loader, default config, content/rules-data namespace, owner-lane state, selector state, target-cardinality state, route-history state, cooldown state, class/commoner-status/personnel/movement/household-mobility/runtime-rule/closeout ledger, projection cache, durable residue, global person scanner, runtime plugin marketplace, arbitrary script surface, reflection-heavy loader, or `PersonRegistry` domain expansion.
- household mobility runtime rule health evidence v549-v556 adds no persisted fields: it is diagnostics/readiness evidence only and adds no module namespace, root/module schema version change, migration, save-manifest membership, rules-data file, rules-data loader, default config, content/rules-data namespace, owner-lane state, selector state, target-cardinality state, route-history state, cooldown state, class/commoner-status/personnel/movement/household-mobility/runtime-rule/health-evidence ledger, projection cache, diagnostic state, durable residue, global person scanner, runtime plugin marketplace, arbitrary script surface, reflection-heavy loader, long-run saturation tuner, performance optimization state, or `PersonRegistry` domain expansion.
- household mobility runtime widening gate v557-v564 adds no persisted fields: it is docs/tests preflight only and adds no module namespace, root/module schema version change, migration, save-manifest membership, rules-data file, rules-data loader, default config, content/rules-data namespace, owner-lane state, selector state, target-cardinality state, route-history state, cooldown state, touch-count state, diagnostic state, performance cache, class/commoner-status/personnel/movement/household-mobility/runtime-rule/widening-gate ledger, projection cache, durable residue, global person scanner, runtime plugin marketplace, arbitrary script surface, reflection-heavy loader, long-run saturation tuner, performance optimization state, or `PersonRegistry` domain expansion.
- household mobility runtime touch-count proof v565-v572 adds no persisted fields: it is focused test evidence only and adds no module namespace, root/module schema version change, migration, save-manifest membership, rules-data file, rules-data loader, default config, content/rules-data namespace, owner-lane state, selector state, target-cardinality state, route-history state, cooldown state, persisted touch-count state, diagnostic state, performance cache, class/commoner-status/personnel/movement/household-mobility/runtime-rule/touch-count-proof ledger, projection cache, durable residue, global person scanner, runtime plugin marketplace, arbitrary script surface, reflection-heavy loader, long-run saturation tuner, performance optimization state, or `PersonRegistry` domain expansion.
- household mobility rules-data fallback matrix v573-v580 adds no persisted fields: it is focused fallback evidence only and adds no module namespace, root/module schema version change, migration, save-manifest membership, rules-data file, rules-data loader, default config, content/rules-data namespace, owner-lane state, selector state, target-cardinality state, route-history state, cooldown state, persisted touch-count state, diagnostic state, performance cache, validation ledger, class/commoner-status/personnel/movement/household-mobility/runtime-rule/fallback-matrix ledger, projection cache, durable residue, global person scanner, runtime plugin marketplace, arbitrary script surface, reflection-heavy loader, long-run saturation tuner, performance optimization state, or `PersonRegistry` domain expansion.
- household mobility runtime tie-break no-touch v613-v620 adds no persisted fields: it is focused test evidence only and adds no module namespace, root/module schema version change, migration, save-manifest membership, rules-data file, rules-data loader, default config, content/rules-data namespace, owner-lane state, selector state, target-cardinality state, route-history state, cooldown state, persisted touch-count state, diagnostic state, performance cache, validation ledger, tie-break ledger, ordering ledger, class/commoner-status/personnel/movement/household-mobility/runtime-rule/tie-break-proof ledger, projection cache, durable residue, global person scanner, runtime plugin marketplace, arbitrary script surface, reflection-heavy loader, long-run saturation tuner, performance optimization state, or `PersonRegistry` domain expansion.
- household mobility runtime pool tie-break no-touch v621-v628 adds no persisted fields: it is focused test evidence only and adds no module namespace, root/module schema version change, migration, save-manifest membership, rules-data file, rules-data loader, default config, content/rules-data namespace, owner-lane state, selector state, target-cardinality state, route-history state, cooldown state, persisted touch-count state, diagnostic state, performance cache, validation ledger, pool tie-break ledger, settlement ordering ledger, active-pool ledger, class/commoner-status/personnel/movement/household-mobility/runtime-rule/pool-tie-break-proof ledger, projection cache, durable residue, global person scanner, runtime plugin marketplace, arbitrary script surface, reflection-heavy loader, long-run saturation tuner, performance optimization state, or `PersonRegistry` domain expansion.
- household mobility runtime score-ordering no-touch v629-v636 adds no persisted fields: it is focused test evidence only and adds no module namespace, root/module schema version change, migration, save-manifest membership, rules-data file, rules-data loader, default config, content/rules-data namespace, owner-lane state, selector state, target-cardinality state, route-history state, cooldown state, persisted touch-count state, diagnostic state, performance cache, validation ledger, score-order ledger, candidate-rank ledger, ordering ledger, class/commoner-status/personnel/movement/household-mobility/runtime-rule/score-order-proof ledger, projection cache, durable residue, global person scanner, runtime plugin marketplace, arbitrary script surface, reflection-heavy loader, long-run saturation tuner, performance optimization state, or `PersonRegistry` domain expansion.

- black-route depth must not create a standalone `BlackRoute` namespace; current save data for that slice already stays inside `OrderAndBanditry` and `TradeAndIndustry`
- any future black-route migration steps must therefore preserve the same module-key set unless an explicit module-addition migration introduces `WarfareCampaign` or another documented pack

## Success criteria
A new module is schema-ready only if:
- it has a namespace
- it has a version
- it has default-state creation
- it has migration notes
- it does not require rewriting unrelated module blobs
- household mobility runtime threshold no-touch v581-v588 adds no persisted fields, namespace, manifest membership, loader, route-history state, movement ledger, selector watermark, target-cardinality state, owner-lane ledger, cooldown ledger, diagnostic state, performance cache, or module schema bump; `PopulationAndHouseholds` remains schema `3`.
- household mobility runtime zero-cap no-touch v589-v596 adds no persisted fields, namespace, manifest membership, loader, route-history state, movement ledger, selector watermark, target-cardinality state, owner-lane ledger, cooldown ledger, diagnostic state, performance cache, or module schema bump; `PopulationAndHouseholds` remains schema `3`.
- household mobility runtime zero-risk-delta no-touch v597-v604 adds no persisted fields, namespace, manifest membership, loader, route-history state, movement ledger, selector watermark, target-cardinality state, owner-lane ledger, cooldown ledger, diagnostic state, performance cache, or module schema bump; `PopulationAndHouseholds` remains schema `3`.
- household mobility runtime candidate-filter no-touch v605-v612 adds no persisted fields, namespace, manifest membership, loader, route-history state, movement ledger, selector watermark, target-cardinality state, owner-lane ledger, cooldown ledger, diagnostic state, performance cache, or module schema bump; `PopulationAndHouseholds` remains schema `3`.
- household mobility runtime tie-break no-touch v613-v620 adds no persisted fields, namespace, manifest membership, loader, route-history state, movement ledger, selector watermark, target-cardinality state, owner-lane ledger, cooldown ledger, tie-break ledger, ordering ledger, diagnostic state, performance cache, or module schema bump; `PopulationAndHouseholds` remains schema `3`.
- household mobility runtime pool tie-break no-touch v621-v628 adds no persisted fields, namespace, manifest membership, loader, route-history state, movement ledger, selector watermark, target-cardinality state, owner-lane ledger, cooldown ledger, pool tie-break ledger, settlement ordering ledger, active-pool ledger, diagnostic state, performance cache, or module schema bump; `PopulationAndHouseholds` remains schema `3`.
- household mobility runtime score-ordering no-touch v629-v636 adds no persisted fields, namespace, manifest membership, loader, route-history state, movement ledger, selector watermark, target-cardinality state, owner-lane ledger, cooldown ledger, score-order ledger, candidate-rank ledger, ordering ledger, diagnostic state, performance cache, or module schema bump; `PopulationAndHouseholds` remains schema `3`.
- household mobility runtime pool-priority no-touch v637-v644 adds no persisted fields, namespace, manifest membership, loader, route-history state, movement ledger, selector watermark, target-cardinality state, owner-lane ledger, cooldown ledger, pool-priority ledger, cross-pool score ledger, active-pool ledger, diagnostic state, performance cache, or module schema bump; `PopulationAndHouseholds` remains schema `3`.
- household mobility runtime per-pool cap no-touch v645-v652 adds no persisted fields, namespace, manifest membership, loader, route-history state, movement ledger, selector watermark, target-cardinality state, owner-lane ledger, cooldown ledger, per-pool cap ledger, global household cap ledger, active-pool ledger, diagnostic state, performance cache, or module schema bump; `PopulationAndHouseholds` remains schema `3`.
- household mobility runtime threshold-event no-touch v653-v660 adds no persisted fields, namespace, manifest membership, loader, route-history state, movement ledger, selector watermark, target-cardinality state, owner-lane ledger, cooldown ledger, threshold-event ledger, event-routing ledger, migration-started selector state, diagnostic state, performance cache, or module schema bump; `PopulationAndHouseholds` remains schema `3`.
- household mobility runtime event-metadata no-prose v661-v668 adds no persisted fields, namespace, manifest membership, loader, route-history state, movement ledger, selector watermark, target-cardinality state, owner-lane ledger, cooldown ledger, event-metadata ledger, prose-parsing ledger, event-routing ledger, migration-started selector state, diagnostic state, performance cache, or module schema bump; `PopulationAndHouseholds` remains schema `3`.
- household mobility runtime event-metadata replay v669-v676 adds no persisted fields, namespace, manifest membership, loader, route-history state, movement ledger, selector watermark, target-cardinality state, owner-lane ledger, cooldown ledger, event-replay state, event-metadata ledger, event-routing ledger, migration-started selector state, diagnostic state, performance cache, or module schema bump; `PopulationAndHouseholds` remains schema `3`.
- household mobility runtime threshold extraction v677-v684 adds no persisted fields, namespace, manifest membership, loader, rules-data file, content/config namespace, route-history state, movement ledger, selector watermark, target-cardinality state, owner-lane ledger, cooldown ledger, threshold-extraction state, event-routing ledger, validation ledger, diagnostic state, performance cache, or module schema bump; `PopulationAndHouseholds` remains schema `3`.
- household mobility runtime candidate-floor extraction v685-v692 adds no persisted fields, namespace, manifest membership, loader, rules-data file, content/config namespace, route-history state, movement ledger, selector watermark, target-cardinality state, owner-lane ledger, cooldown ledger, candidate-floor state, event-routing ledger, validation ledger, diagnostic state, performance cache, or module schema bump; `PopulationAndHouseholds` remains schema `3`.
- household mobility runtime score-weight extraction v693-v700 adds no persisted fields, namespace, manifest membership, loader, rules-data file, content/config namespace, route-history state, movement ledger, selector watermark, target-cardinality state, owner-lane ledger, cooldown ledger, score-weight state, event-routing ledger, validation ledger, diagnostic state, performance cache, or module schema bump; `PopulationAndHouseholds` remains schema `3`.
- household mobility runtime labor-floor extraction v701-v708 adds no persisted fields, namespace, manifest membership, loader, rules-data file, content/config namespace, route-history state, movement ledger, selector watermark, target-cardinality state, owner-lane ledger, cooldown ledger, labor-floor state, event-routing ledger, validation ledger, diagnostic state, performance cache, or module schema bump; `PopulationAndHouseholds` remains schema `3`.
- household mobility runtime grain-floor extraction v709-v716 adds no persisted fields, namespace, manifest membership, loader, rules-data file, content/config namespace, route-history state, movement ledger, selector watermark, target-cardinality state, owner-lane ledger, cooldown ledger, grain-floor state, event-routing ledger, validation ledger, diagnostic state, performance cache, or module schema bump; `PopulationAndHouseholds` remains schema `3`.
- household mobility runtime land-floor extraction v717-v724 adds no persisted fields, namespace, manifest membership, loader, rules-data file, content/config namespace, route-history state, movement ledger, selector watermark, target-cardinality state, owner-lane ledger, cooldown ledger, land-floor state, event-routing ledger, validation ledger, diagnostic state, performance cache, or module schema bump; `PopulationAndHouseholds` remains schema `3`.
- household mobility runtime grain-divisor extraction v725-v732 adds no persisted fields, namespace, manifest membership, loader, rules-data file, content/config namespace, route-history state, movement ledger, selector watermark, target-cardinality state, owner-lane ledger, cooldown ledger, grain-divisor state, event-routing ledger, validation ledger, diagnostic state, performance cache, or module schema bump; `PopulationAndHouseholds` remains schema `3`.
- household mobility runtime land-divisor extraction v733-v740 adds no persisted fields, namespace, manifest membership, loader, rules-data file, content/config namespace, route-history state, movement ledger, selector watermark, target-cardinality state, owner-lane ledger, cooldown ledger, land-divisor state, event-routing ledger, validation ledger, diagnostic state, performance cache, or module schema bump; `PopulationAndHouseholds` remains schema `3`.
- household mobility runtime candidate-ceiling extraction v741-v748 adds no persisted fields, namespace, manifest membership, loader, rules-data file, content/config namespace, route-history state, movement ledger, selector watermark, target-cardinality state, owner-lane ledger, cooldown ledger, candidate-ceiling state, event-routing ledger, validation ledger, diagnostic state, performance cache, or module schema bump; `PopulationAndHouseholds` remains schema `3`.
- household mobility runtime distress-trigger extraction v749-v756 adds no persisted fields, namespace, manifest membership, loader, rules-data file, content/config namespace, route-history state, movement ledger, selector watermark, target-cardinality state, owner-lane ledger, cooldown ledger, distress-trigger state, event-routing ledger, validation ledger, diagnostic state, performance cache, or module schema bump; `PopulationAndHouseholds` remains schema `3`.
- household mobility runtime debt-trigger extraction v757-v764 adds no persisted fields, namespace, manifest membership, loader, rules-data file, content/config namespace, route-history state, movement ledger, selector watermark, target-cardinality state, owner-lane ledger, cooldown ledger, debt-trigger state, event-routing ledger, validation ledger, diagnostic state, performance cache, or module schema bump; `PopulationAndHouseholds` remains schema `3`.
- household mobility runtime labor-trigger extraction v765-v772 adds no persisted fields, namespace, manifest membership, loader, rules-data file, content/config namespace, route-history state, movement ledger, selector watermark, target-cardinality state, owner-lane ledger, cooldown ledger, labor-trigger state, event-routing ledger, validation ledger, diagnostic state, performance cache, or module schema bump; `PopulationAndHouseholds` remains schema `3`.
- household mobility runtime grain-trigger extraction v773-v780 adds no persisted fields, namespace, manifest membership, loader, rules-data file, content/config namespace, route-history state, movement ledger, selector watermark, target-cardinality state, owner-lane ledger, cooldown ledger, grain-trigger state, event-routing ledger, validation ledger, diagnostic state, performance cache, or module schema bump; `PopulationAndHouseholds` remains schema `3`.
- household mobility runtime land-trigger extraction v781-v788 adds no persisted fields, namespace, manifest membership, loader, rules-data file, content/config namespace, route-history state, movement ledger, selector watermark, target-cardinality state, owner-lane ledger, cooldown ledger, land-trigger state, event-routing ledger, validation ledger, diagnostic state, performance cache, or module schema bump; `PopulationAndHouseholds` remains schema `3`.
- household mobility runtime livelihood-trigger extraction v789-v796 adds no persisted fields, namespace, manifest membership, loader, rules-data file, content/config namespace, route-history state, movement ledger, selector watermark, target-cardinality state, owner-lane ledger, cooldown ledger, livelihood-trigger state, event-routing ledger, validation ledger, diagnostic state, performance cache, or module schema bump; `PopulationAndHouseholds` remains schema `3`.
- household mobility runtime livelihood-score extraction v797-v804 adds no persisted fields, namespace, manifest membership, loader, rules-data file, content/config namespace, route-history state, movement ledger, selector watermark, target-cardinality state, owner-lane ledger, cooldown ledger, livelihood-score state, event-routing ledger, validation ledger, diagnostic state, performance cache, or module schema bump; `PopulationAndHouseholds` remains schema `3`.
- household mobility runtime pressure-score extraction v805-v812 adds no persisted fields, namespace, manifest membership, loader, rules-data file, content/config namespace, route-history state, movement ledger, selector watermark, target-cardinality state, owner-lane ledger, cooldown ledger, pressure-score state, event-routing ledger, validation ledger, diagnostic state, performance cache, or module schema bump; `PopulationAndHouseholds` remains schema `3`.
- household mobility runtime migration-status extraction v813-v820 adds no persisted fields, namespace, manifest membership, loader, rules-data file, content/config namespace, route-history state, movement ledger, selector watermark, target-cardinality state, owner-lane ledger, cooldown ledger, migration-status state, event-routing ledger, validation ledger, diagnostic state, performance cache, or module schema bump; `PopulationAndHouseholds` remains schema `3`.
- household mobility runtime migration-risk clamp extraction v821-v828 adds no persisted fields, namespace, manifest membership, loader, rules-data file, content/config namespace, route-history state, movement ledger, selector watermark, target-cardinality state, owner-lane ledger, cooldown ledger, risk-clamp state, event-routing ledger, validation ledger, diagnostic state, performance cache, or module schema bump; `PopulationAndHouseholds` remains schema `3`.
- household mobility runtime tie-break priority extraction v829-v836 adds no persisted fields, namespace, manifest membership, loader, rules-data file, content/config namespace, route-history state, movement ledger, selector watermark, target-cardinality state, owner-lane ledger, cooldown ledger, tie-break state, ordering ledger, event-routing ledger, validation ledger, diagnostic state, performance cache, or module schema bump; `PopulationAndHouseholds` remains schema `3`.
- household mobility runtime unmatched livelihood score extraction v837-v844 adds no persisted fields, namespace, manifest membership, loader, rules-data file, content/config namespace, route-history state, movement ledger, selector watermark, target-cardinality state, owner-lane ledger, cooldown ledger, unmatched-livelihood state, ordering ledger, event-routing ledger, validation ledger, diagnostic state, performance cache, or module schema bump; `PopulationAndHouseholds` remains schema `3`.
