# Exec plans

Place active execution plans in `active/`.
Move completed plans to `archive/`.

Every plan should include:
- goal
- in/out scope
- touched modules
- schema/save impact
- determinism risk
- milestones
- tests
- rollback path
